# Session handoff — клиентский трек (СТАТУС: ожидает Этап 0)

> Первое сообщение для новой сессии клиентского трека (Presentation/UI/арт/game feel).
> Прочитать в начале сессии, обновить в конце. Файл живёт в репо и не удаляется —
> история версий = история трека.
>
> **Трек ещё НЕ активен:** Unity-проект появится в `client/` на Этапе 0 (server-трек);
> первая клиентская работа — game-feel-пасс Этапа 1. До того сессии не запускать.

**Дата handoff:** 2026-07-31 (подготовка проекта; кода ещё нет).

---

## 1. Состояние bd (общий трекер `.beads/` этого репо, префикс `app-`)

- Эпики этапов 0–5 созданы blocks-цепочкой: `app-4yd` (Э0) → `app-88s` (Э1) →
  `app-5nu` (Э2) → `app-35g` (Э3) → `app-4bi` (Э4) → `app-hfa` (Э5, граница MVP).
- Клиентские задачи появятся как issues внутри эпиков начиная с Этапа 1
  (game-feel-пасс) — их заведёт plan-фаза Этапа 1.

## 2. Контекст

Проект: PvEvP extraction-арена «Кольцо» — концепт в `docs/adr/ADR-001-Концепт.md`
(§4.1 — социальная динамика: войс и короткие фразы обязательны в MVP), техника в
`docs/adr/ADR-002-Разработка.md`, сеттинг/визуальный язык/словарь — в
`docs/adr/ADR-003-Сеттинг.md`. Правила клиентского трека — `client/CLAUDE.md`
(зоны, незыблемые правила, game-feel-чеклист, процесс).
Граница enforce'ится CODEOWNERS + branch protection: PR в server-ownership пути
не смержится без апрува владельца.

## 3. ⭐ Задача первой сессии трека (когда активируют)

Определяется планом Этапа 1 (`app-88s`): game-feel-пасс ядра боёвки по чеклисту
ADR-001 §10. Это фаза «человек в цикле»: вкус владельца важнее автотестов, все числа —
в ScriptableObjects для hot-tweak в PlayMode. При активации владелец вписывает сюда
конкретные issues и repository state.

## 4. ⭐ Шаги сессии (sequence)

1. Прочитать целиком: этот handoff → `client/CLAUDE.md` → корневой `CLAUDE.md` →
   ADR-001 §9–10 (+ ADR-002 обзорно).
2. `bd ready` → `bd update <id> --claim` по назначенной задаче.
3. Прочитать существующий код `Presentation/` и публичные контракты `Simulation`
   (READ-ONLY) — что уже есть, что переиспользовать.
4. Brainstorm → план → имплементация; плейтест — основной критерий.
5. Верификация: сборка клиента + плейтест (сетевое — с latency simulator 80 мс/5%);
   PR; `bd close` с evidence; обновить этот handoff.

## 5. Что НЕ делать

- ❌ НЕ менять server-ownership код и `Assets/Data` вне отдельного PR (см.
  `client/CLAUDE.md` §Зоны) — потребность = bd-issue server-треку.
- ❌ НЕ принимать решения по игровому дизайну — вопросы вкуса решает владелец.
- ❌ НЕ добавлять ассеты/пакеты без записи в ADR-002 §1.
- ❌ НЕ коммитить бинарники мимо LFS; НЕ коммитить секреты.
- ❌ НЕ TodoWrite/MEMORY.md — трекинг только bd.

## 6. Шаблон обновления (в конце каждой сессии трека)

Сохранить структуру секций 1–5, добавив: recap сделанного (с bd-id и коммитами),
lessons learned (сквозная нумерация), актуальный repository state (HEAD, открытые PR),
задачу следующей сессии. Плотно, фактами, без воды.
