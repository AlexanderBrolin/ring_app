# История задачи `app-wpei` — архив колонки `notes`

**Задача:** Ф3: отмотка и время показа — история, две половины компенсации, трассер, перепин (веха В3)

**Тип:** task · **Приоритет:** P1 · **Статус:** in_progress

⚠ **Это архив, а не рабочий документ.** Колонка `notes` в bd ограничена 65 535
байтами, и у неё есть второй замок (урок 601): `bd update` пишет в журнал событий
полный JSON задачи, поэтому у большой задачи падает **любое** обновление, включая
уменьшающее. Правило трека — архивировать примерно с 50 КБ, **пока `bd update` ещё
проходит**, а не когда он уже отказал. На старте сессии 77 колонка несла 54639 байт,
то есть порог был перейдён. Ниже — колонка `notes` **дословно**, на момент выноса.

**Выгружено:** 2026-08-30 · **объём:** 54639 байт

---

Вход: план v3, таски Т24-Т34, веха В3. Из круга 3: HistorySlot вносится в HashMob И HashPlayer (Step 3a Т24) — без фолда рефлексивный свип красен до конца фазы, а 'пересчитать квитанцию' инертно; квитанция 152 -> 155; Т28 заводит ProjectileState.RewindLeft (носитель k_картинка) с фолдом в HashProjectile, квитанция +1; M28 переехала в Т28 Step 6, её свидетель RewindSurvivesANeighborsDeath написан; Т29/Т32 получают переезд маркер-ключа NetConfig (EntityFadeTicks -> RewindSanityTicks -> TracerCatchUpBudget) — утверждение v2 'NetConfig бутстрапом не доставляется' было ложным; Т30 добавляет поле payload Pos и девятую точку касания IsKnown. Перепин golden — ОДИН, в Т34, санкция израсходуется.
СЕССИЯ 74 (2026-08-29), старт. Сверка §10 сошлась целиком: main 67c7f34, ветка 1280593, обе сверены живым ls-remote, оба дерева чистые (0 строк), stash пуст, worktree два, md5 DeterminismTests.cs 71279f0e9debdba7a247e18e89532e4c, MUTATED/ABLATED ноль, MEASURING одно законное, сцена 269 / 0241c714945a15bf1c68060f075209a4 (с sort), зонная колонка ноль, Unity не запущен. ListAgents: параллельной сессии трека нет. РЕШЕНИЯ ВЛАДЕЛЬЦА: (1a) скоуп сессии - ТОЛЬКО Т24; (2г) два фазовых ревьюера гейта Ф2 НЕ запускаются, вместо них ОДИН наряд по Т14 плюс Т23 приложением, и не сейчас, а ПЕРЕД Т28 - отдельной задачей; (3a) развилка I-3 (видимость тел внутри симуляции) остаётся как есть до вехи В4. ⚠ ПОМАРКА ПЛАНА, найденная сверкой: Step 6 Т24 обещает total 1657, но в дереве 1704, значит ждать надо 1708 - число плана писалось до Ф2.
АУДИТ АДРЕСОВ Т24 ЗАКРЫТ (сессия 74, лично, приём урока 543) — эвиденс $SDD/task-24-audit.md. Начат с диффа эпика 3cb379d..1280593 (126 файлов): все семь существующих домов Т24 эпиком тронуты, пофайловая сверка обязательна. Адреса уехали: HashPlayer 2621 -> 2970, HashMob 2674 -> 3037, своп с хвостом 1607 -> 1867; SpawnMob 2303, DamageMob 1737 (девять параметров), KillPlayer 2186. ЧЕТЫРЕ РАСХОЖДЕНИЯ ПЛАНА С КОДОМ, ДВА НЕСУЩИХ. A-1 (Critical): два новых скаляра ArenaSimConfig обязаны войти в SimConfigHash.HashArena — свип EveryConfigNumberAffectsHash_Arena бампает КАЖДОЕ скалярное поле секции, скип-лист содержит только имена массивов; без фолда это ЧЕТВЁРТЫЙ КРАСНЫЙ, а Files Т24 SimConfigHash.cs не называет. Прецедент — Т19 §6, где тот же фолд сделан сверх буквы брифа и признан верным. A-2 (Critical): маркер-ключ арены сегодня RelaxIterations (ArenaConfig.cs:420, аргумент StageOneSceneBootstrap.cs:1085), два поля Т24 ложатся после него ⇒ нужны четыре вещи и R-APPLY/R-IDEM; тело таска их не называет, ХОТЯ Global Constraints плана называют Т24 поимённо — план противоречит сам себе, класс Н-38, пятый раз подряд после Н-38, Н-39, рулингов 119 и 122. A-3 (Important): квитанция WorldLifecycleTests устарела — в ней 152, факт 154, потому что PlayerState несёт 35 полей, а не 34: SlideSpeedPenalty (решение владельца Р443, Т22) в квитанцию не внесён. Ожидание Step 4 — 154 -> 157, а не 152 -> 155 из плана; счёт снят свежим разбором всех девяти хешируемых структур, а не прочтением комментария. Исполняемого следствия нет — квитанция это комментарий, красным тест делает рефлексивный свип. A-4 (Important): фикстура TestConfigs.DefaultArena() обязана нести те же 6 и 3 (Р117, Т22 своё RelaxIterations туда положил) ⇒ TestConfigs.cs входит в скоуп. ДОТАСКОВОЕ, записано и не чинится: ConfigTests.AssertArenaEqual не сравнивает RelaxIterations — рукописный компаратор уже на поле позади, класс Ruling 2 / Н-34. РУЛИНГИ 123-126: 123 фолд двух полей арены в HashArena в этом же таске, в конец; 124 StageOneSceneBootstrap входит в скоуп, таск получает R-APPLY, коммит артефактов и R-IDEM; 125 квитанция пересчитывается целиком свежим typeof(X).GetFields(), ожидание 157, правка обязана назвать пропущенное поле Т22; 126 фикстура получает RewindCapTicks 6 и RewindPictureTicks 3. ПОДТВЕРЖДЕНО СВЕРХ СПИСКА: TicksFromSeconds(0.2f) = 6 (тело round(seconds/TickDt)), и док метода сам называет цену умножения; девятипараметровый DamageMob совпал со сниппетами плана; тест-швы Mobs, SetMobForTest, PlayerAt, SetPlayerForTest, SpawnMobsAt, Open/OpenField — все на месте. Кода не написано, дерево ветки чистое.
Т24 ЗАКРЫТ, коммит 8b0874e (ветка feature/app-88jb-weapon-netcode, НЕ ЗАПУШЕНА). Тестов 1704 -> 1709 (+5), зелёных 1706, красных РОВНО ТРИ - эталоны, и все три сдвинулись как обязаны были: GoldenHash 9595197576853725280 -> 13365544860976558584, Extraction 9223590669344026349 -> 17162214255381334543, MultiPlayer 8810226901549957896 -> 337319130742352515. R-APPLY EXIT=0 - ArenaConfig.asset получил RewindCapTicks 6 и RewindPictureTicks 3 (до Apply их там НЕ БЫЛО, то есть переезд маркер-ключа RelaxIterations -> RewindPictureTicks окупился); R-IDEM EXIT=0, git status и git diff по client/ по нулю строк; сцена 269 / 0241c714945a15bf1c68060f075209a4 до и после обоих Apply. Свипы дважды: кириллица вне литералов 0, британизмы 0, секреты 0. ГЕЙТ-META: оба GUID уникальны. ГЕЙТ-КОДОГЕН прогнан и чист - и он был НУЖЕН, см. урок 605. Мутаций 8, семь с названной жертвой. ЧЕТЫРЕ ВЕЩИ, КОТОРЫЕ СТОИТ ЗНАТЬ СЛЕДУЮЩЕЙ СЕССИИ. (1) Н-46: аудит, снятый грепом, не аудит - утверждение аудита A-7 <ни один тест не делает сохранить-откатить-заспавнить-сверить хеш> оказалось ложным, такой тест есть (WorldLifecycleTests.SaveRestore_ReplaysToSameHash:35-47), спавн там делают волны внутри циклов, а не строка с именем SpawnMob. Код был бы сдан с ЧЕТВЁРТЫМ КРАСНЫМ; нашёл исполнитель и сдал дерево с красным, а не починил тихо. (2) РУЛИНГ 133: выдача слота - наименьший свободный, занятое множество пересобирается из живых тел в RestoreState. Довод - требование отката: свободный список делает слот функцией истории возвратов, то есть состоянием вне сохранения. Формат сейва не тронут, Т25 остаётся должен САМИ СТРОКИ кольца в хеш и в сохранение. (3) РУЛИНГ 137: мутация <вернуть слот после свопа> не убила НИКОГО - порча детерминированная и симметричная, а тест на детерминизм такого не видит по построению. Написан пятый тест NoTwoLiveBodies_ShareAHistorySlot, повтор мутации убил его вторым ассертом. (4) УРОК 605: PlayerState едет на провод целиком через ReconcileData, поэтому провод меняется БЕЗ единой правки в Networking/**; критерий <трогали ли сетевые файлы> ложен, верный - <меняли ли тип, который сериализует кодоген>. Бампа ProtocolVersion нет, прецедент Т22. Рулинги 123-137 и разбор - в task-24-audit.md и progress.md сессии 74. Заведено: app-41wd (side-quest, SetMobForTest подсовывает чужой слот, латентно).
КРУГ РЕВЬЮ Т24 И КРУГ ПРАВОК ЗАКРЫТЫ, коммит b556880 (запушен). Два ревьюера, разные углы, CRITICAL ноль, ЛОЖНЫХ НОЛЬ. Нашли ТРИ строки продакшн-кода без свидетеля и ДЕВЯТЬ комментариев, лгущих о коде, из которых ШЕСТЬ написал сам коммит Т24 - и всё это ПОСЛЕ восьми мутаций, свипов и четырёх полных прогонов. Тестов 1715 / 1712 зелёных / ровно три эталона, числа эталонов те же, что после Т24 (круг мира не касается). Мутаций круга шесть, все с названной жертвой. R-IDEM пуст, гейт кодогена ноль по четырём сборкам, сцена 269 / 0241c714945a15bf1c68060f075209a4. ГЛАВНОЕ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ. (1) Н-47: HistorySlot_SurvivesASwapRemoveOfANeighbor, объявленный планом свидетелем Р406 и прошедший три круга self-review, мерил семантику копирования структуры и оставался зелёным при адрес=индекс массива - при той самой схеме, которую Р406 отвергает. Усилен; мутация M-addr до усиления не убивала НИЧЕГО, после убивает три теста. Класс находки: тест, чьё ИМЯ описывает инвариант, а ассерт - языковую гарантию. (2) Н-48 и урок 606: новое правило NetInvariants #11 уронило чужую фикстуру InterpBufferTicksZero_IsReported, а мини-раннер исполнителя этого не увидел, потому что ВЕСЬ сьют NetInvariantsTests стоит на ScriptableObject и лежит в его слепой зоне целиком. Рулинг 136 усилен решением владельца: список слепых зон - постоянной записью в task-24-review-evidence.md, и отчёт обязан называть их ДО вердикта. (3) Урок 605: PlayerState едет на провод целиком через ReconcileData, поэтому критерий трогали-ли-мы-Networking ЛОЖЕН; верный - меняли ли тип, который сериализует кодоген. (4) Урок 607: правило вида A == B требует ДВУХ позитивных свидетелей - на отгруженной паре и на уведённой вместе, иначе фикстура на отгруженных числах не отличает связь от константы. (5) NetInvariants #11 ГЕЙТИТ СТАРТ СЕРВЕРА (ServerBootstrap зовёт Validate в бою); отгружены 3 и 3, проверено по обоим .asset. Рулинги 138-142 и разбор - в task-24-review-evidence.md и progress.md. Спека получила §6d: расхождение §3.6 с Р406 про счётчик населения строки записано фактом, правка §3.6 - решение владельца.
СЕССИЯ 75, старт. Сверка §10 сошлась целиком: main c044946, ветка b556880, обе сверены живым ls-remote, оба дерева чистые (0 строк), stash пуст, worktree два, md5 DeterminismTests.cs 71279f0e9debdba7a247e18e89532e4c, MUTATED ноль, сцена 269 / 0241c714945a15bf1c68060f075209a4 (с sort), Unity не запущен, lockfile отсутствует, открытых PR нет. ListAgents: параллельной сессии трека нет. АУДИТ Т25 СНЯТ ЛИЧНО, приёмом урока 543, с диффа 1280593..b556880 - эвиденс task-25-audit.md. НАХОДКИ. (1) Три адреса из четырёх в handoff устарели на 3 строки: их снимали на 8b0874e, а круг правок b556880 добавил три строки в док и текст исключения ApplyConfig. Факт дерева: TickAll 461 (совпал), SaveState 2718, RestoreState 2785, StateHash 3037; место фолда истории - между 3062 и 3070. (2) A-Т25-1, несущее: форма фолда Р409 требует <длина + записи>, но Т24 счётчик населения строки НЕ ЗАВЁЛ и это записанное решение (спека §6d, док класса) - при постоянном слоте считать нечего. Что именно фолдится под словом длина и что фолдить для строки с несовпавшим штампом, не определено ни планом, ни спекой. Нужно решение координатора ДО брифа. (3) A-Т25-2: StateHash зовётся ПО ТИКУ в боевом пути (LocalSimBackend), а наивный фолд всех семи строк по 1353 слота - это 9471 запись на вызов; риск Р-B реален, форма фолда обязана быть ограничена населением, а не ёмкостью кольца. (4) УТОЧНЕНИЕ к handoff: app-41wd на Т25 красным НЕ становится - три рукописных сборщика MultiPlayerWorldTests не тикают ни разу, а рефлексивный проход HotTweakTests после записи HistorySlot=1000000 зовёт только ApplyConfig, ни Tick, ни RestoreState. Семантически задача остаётся, наблюдаемого красного нет. (5) Т25 обязан завести конструктор Record и оба тела Write/PosAt, плюс перестать делать ложью СЕМЬ доков Т24 о собственной границе. Кода не написано, дерево ветки чистое.
Т25 КРАСНАЯ ФАЗА ЗАКРЫТА, предсказание сошлось целиком. Прогон координатора лично, фильтр RewindTests, 25 c, EXIT=2, error CS 0, Csc 8. testcasecount фильтра 12, total 12, passed 8, failed 4. ВЛОЖЕННЫЙ сьют ring-client-new дал 1720 - счёт проекта снят машиной и подтвердил рулинг 145 (пять тестов вместо трёх из плана, потому что PosAt получает свидетеля на каждую ветку своей таблицы). Красные ровно четыре и те самые: TwoWorldsWithEqualPresentAndDifferentPast (единственный ассерт), HistoryRowOfTickT (первый), PosAtATickWithNoRow (первый), PosAtATickTheCollectorDidNotSurvive (второй, положительный). ГЛАВНОЕ: у первого теста ОБА числа хеша равны бит в бит, 15093650921087565744 против того же самого - то есть контрпример Р380 воспроизведён машиной, два мира с разной историей за три тика дают ОДИН хеш. Наряд A вернул девять расхождений, все состоятельны, ложных ноль. Рулинги 147-151: 147 гард tick<0 до всякой индексации, потому что -1 % 7 = -1 в C# и Р378 безусловен - находка исполнителя поймала дефект и в моём рулинге 143 (окно фолда на первых шести тиках уходило в отрицательный индекс и уронило бы StateHash); 148 шов PlayerHistorySlotForTest снят как дубль живой конвенции файла, прецедент Т4; 149 устаревший счёт соседей правится здесь, сосед называется по имени; 150 британизмы в открытых файлах чиним, восемь metres в чужих - side-quest; 151 FreezeArchetype остаётся private. Заведена app-uko9 (P3): свип британизмов чувствителен к регистру и слеп к капсу - CANCELLED в PositionHistory.cs пережил четыре прогона и два круга ревью, а строчное neighbour там же свип по диффу НАХОДИТ, значит финальный свип круга правок по собственному новому тексту не гнался. Обе строки из коммита b556880, то есть из круга правок, чей урок 608 празднует ровно этот свип.
Т25 ЗЕЛЁНАЯ ФАЗА ЗАКРЫТА, предсказание сошлось целиком, эвиденс task-25-green-evidence.md. Фильтр RewindTests: EXIT=0, 12/12, 25 c, Csc 8. ПОЛНЫЙ ПРОГОН: EXIT=2, 370 c, testcasecount 1720, зелёных 1717, красных РОВНО ТРИ, error CS 0, Csc 0 (правок между прогонами не было - норма). ТРИ ЭТАЛОНА СДВИНУЛИСЬ ВСЕ ТРИ, санкционировано: GoldenHash 13365544860976558584 -> 6179821658852201134, MultiPlayer 337319130742352515 -> 1839905722486512695, Extraction 17162214255381334543 -> 5806527071770847608. Пины Expected не двигались. ЧЕТВЁРТОГО КРАСНОГО НЕТ. Сдвиг трёх из трёх и есть свидетельство что фолд живой - предсказание содержало отрицательную половину по уроку 580: несдвинувшийся эталон означал бы мёртвый фолд и стоп. Место возможного четвёртого красного было названо ДО прогона (Write не имеет гарда на слот, любая фикстура со слотом вне диапазона плюс тик уронила бы его) - не уронила ни одна, то есть уточнение аудита про app-41wd подтверждено прогоном, а не согласием. ВРЕМЯ ЗАМЕРЕНО: 370 c против полосы Т24 365-390, рост фолда на этом разрешении не обнаруживается, LA на старте 6.11. Честно названо что это НЕ гейт Р433: полного прогона родительского коммита сессия не снимала, материал по классам записан в эвиденс (AllocationTests 202.4 c и DeterminismTests 139.2 c дают 98 процентов времени всего сьюта). Наряд B вернул семь расхождений, все состоятельны: предикаты подката и неуязвимости совпали с живым боевым путём ДОСЛОВНО (SlideTimer > 0f из AcceptCandidate:711 и IframeTimer > 0f из DamagePlayer:2043), причём наряд ошибался - неуязвимость читает не AcceptCandidate, а Update:504; найдено восьмое лгущее место (док поля _history в SimulationWorld) и девятое (neighbour в PositionHistory, которого рулинг 150 не назвал); записан новый факт кода - ReturnSlot стирает прошлое умершего РЕТРОАКТИВНО, значит отмотка не достанет моба, умершего позже.
Т25 ЗАКРЫТ КОДОМ, коммит 67ef01b. ЧИСТЫЙ ПОЛНЫЙ ПРОГОН НА ТОЧНЫХ БАЙТАХ КОММИТА: EXIT=2, 365 c, testcasecount 1721, зелёных 1718, красных РОВНО ТРИ, error CS 0. ЭТАЛОНЫ (сравнивать But was с But was): GoldenHash 6179821658852201134, MultiPlayer 1839905722486512695, Extraction 5806527071770847608. Пины Expected не двигались, перепин остаётся один в Т34. Подлинность прогона доказана не кодом возврата, а числами: под мутацией M37 те же три эталона давали 16909799270401317950 / 2517864086022746797 / 13869568825671289730, после отката вернулись к чистым - значит откатанные байты реально скомпилированы. МУТАЦИИ СЕМЬ, предсказания сошлись 7 из 7. Пять убиты названной жертвой на названном ассерте: M32 пишет историю до движения (жертва HistoryRowOfTickT, АССЕРТ 2, расхождение 0.04444885 м против предсказанных счётом 0.044), M33 снимает фолд (жертва TwoWorlds, и дайджест вернулся к числу КРАСНОЙ ФАЗЫ 15093650921087565744 бит в бит - доказано что GREEN добавил в StateHash ровно один шаг), M34 копия ссылкой (жертва SaveAndRestore), M35 снят гард tick<0 (жертва новый тест 13, IndexOutOfRangeException), M36 снят гард t>=0 в фолде (жертва TwoWorlds как Error). ДВЕ ПРЕДСКАЗАНЫ ВЫЖИВШИМИ И ВЫЖИЛИ: M37 (фолдить присутствие вместо длины) и M38 (порядок обхода тел). Полный прогон под M37 замерен отдельно: 1721 теста, 1718 зелёных, ровно три красных - то есть мутация, чей единственный эффект другое число дайджеста, проходит ВЕСЬ сьют. НАХОДКА, записанная сейчас а не когда заболит: форму фолда и канонический порядок обхода не пиннит ничто, кроме трёх эталонов, а они красны намеренно до Т34. Это НЕ случай рулинга 137: там мутация вносила настоящую порчу невидимую тестам, здесь порчи нет вовсе (n и present информационно эквивалентны, порядок обхода произвольная конвенция). Различать надо мутацию без жертвы и мутацию без дефекта. РУЛИНГ 152: гард tick<0 не имел свидетеля вовсе - тест 11 спрашивает тик 0, а ноль неотрицателен; заведён тринадцатый тест PosAtANegativeTick_DegradesInsteadOfThrowing, свидетелем его сделала мутация M35, прецедент рулинг 137. ГЕЙТЫ: свипы кириллицы и британизмов по диффу - ноль, и с -i тоже ноль (Н-49); секрет-чек пуст; новых файлов нет, значит .meta не нужен; Data/ не тронут, значит R-APPLY/R-IDEM не нужны; сцена 269 / 0241c714945a15bf1c68060f075209a4 не двигалась; ГЕЙТ-КОДОГЕН ноль во всех четырёх сборках, SimStates.cs не тронут ни строкой, ProtocolVersion остаётся 4; git diff --cached --stat - ровно четыре файла скоупа, +675/-55.
КРУГ РЕВЬЮ Т25 И КРУГ ПРАВОК ЗАКРЫТЫ, коммит 1500a76. Два ревьюера, Critical 1, Important 3, Minor 9, ЛОЖНЫХ НОЛЬ; каждая находка перепроверена координатором лично открытием файла, обе ложные ветки сняли сами ревьюеры с основанием по коду. CRITICAL и это промах координатора: вся ось Flags стояла без единого свидетеля - ассертов на Record.Flags во всём дереве ноль, все пять вызовов PosAt шли со слотом сборщика, ни одна фикстура не подкатывалась и не дэшилась, а косвенный путь закрыт замером M37 (правка, меняющая только дайджест, проходит весь сьют). Свидетель был НАЗНАЧЕН СПЕКОЙ заранее: §4.3 несёт 35 бит Sliding, рулинг 145 подтянул соседей 34 и 36 и не подтянул 35. IMPORTANT A-1: PosAt научилась ЛГАТЬ - slot не проверяется нигде и на совпавшем штампе возвращает чужую запись как свою, а шапка обещала безусловно; до Т25 это было правдой, потому что тело было заглушкой. РУЛИНГ 154: гарда НЕ ставим, довод внешний - сосед RederiveOccupancy ровно этот контракт записал а не загардил, и гард был бы продакшн-кодом чей красный тест означал бы фикстуру, воспроизводящую app-41wd. Обещание сужено до tick, обе ветки названы, app-41wd расширена ТРЕТЬИМ швом (ветка молчаливой лжи её описанием не покрывалась - там записано падение). IMPORTANT B-2 и B-3, плюс девять текстовых правок, одна из которых про довод самого координатора: обоснование формы фолда ссылалось на конфигурацию, которой не бывает - присутствие строки МОНОТОННО и есть чистая функция _tick, а он фолдится первым шагом, то есть по критерию Р114 нулю равна различающая сила ВСЕГО шага n. ЧИСЛА: фильтр 16/16 EXIT=0; полный прогон EXIT=2, 375 c, total 1724, зелёных 1721, красных ровно три, error CS 0. ТРИ ЭТАЛОНА БИТ В БИТ ПРЕЖНИЕ (5806527071770847608 / 6179821658852201134 / 1839905722486512695) - круг поведения не менял, и вынос повторённой арифметики этим доказан нейтральным. МУТАЦИЙ КРУГА СЕМЬ, сошлись 7 из 7: пять оси Flags убиты названной жертвой на названном ассерте, M45 убила четыре теста. M39 была фальсификатором находки B-1 по прямому указанию ревьюера и подтвердила её - один красный из шестнадцати, все тринадцать прежних зелёные. M44 ПРЕДСКАЗАНА ВЫЖИВШЕЙ И ВЫЖИЛА - находка исполнителя про сам рефактор рулинга 156: общий хелпер делает симметричную порчу невидимой, до выноса три написания давали три асимметричные мишени, после выноса мишень возвращает только асимметричная мутация. Тот же класс, что рулинг 137, на другом материале: reuse больше duplication верно, но цена - потеря мишеней, и её надо называть. ГЕЙТЫ: свип поймал русскую прозу в .cs (цитата из спеки) ПЯТЫЙ РАЗ ПОДРЯД перед коммитом - исправлено, перепрогнано; британизмы 0 и без -i и с -i; секреты 0; новых файлов 0; Data не тронут; сцена 269 не двигалась; кодоген 0 во всех четырёх сборках.
СЕССИЯ 76, старт. Сверка §10 сошлась целиком: main 8bb5afb, ветка 1500a76, обе сверены живым ls-remote, оба дерева чистые (0 строк), stash пуст, worktree два, md5 DeterminismTests.cs 71279f0e9debdba7a247e18e89532e4c, MUTATED ноль, сцена 269 / 0241c714945a15bf1c68060f075209a4 (с sort), Unity не запущен, lockfile отсутствует, открытых PR нет. ListAgents показал пиринговую сессию the-ring-c4 = сессия 75; ответила, что закрыта и репозиторий не трогает, её отчёт НЕ принят на веру — вся таблица git снята мной ДО её ответа и совпала (урок 518). АУДИТ Т26 СНЯТ ЛИЧНО, приёмом урока 543, с диффа эпика 3cb379d..1500a76 (133 файла) — эвиденс task-26-audit.md. ПЯТЬ НАХОДОК. A-Т26-1 (Critical): формула заполнения глубины из плана и спеки §3.7 вычитает тики РАЗНЫХ ДОМЕНОВ — RenderClock и NetworkSimBackend.CurrentTick считают в тиках МИРА (оба говорят это своими доками), а предсказание клиента живёт в тике FishNet (ReconcileData берёт TimeManager.LocalTick, гост шагает по нему же), моста нет, и MatchServer несёт шрам критического фикса C1 ровно за такое вычитание. Это Н-11 на другом материале. A-Т26-2 (Critical): рефлексивных свипов по SimInput в дереве ДВА, а не один — второй ReconcileCodecTests:33 через SimInputFieldsCovered:160 и сверку :387, и он краснеет на шаге 2 ⇒ КРАСНЫХ ПЯТЬ, А НЕ ЧЕТЫРЕ; рядом рукописный AssertSimInputEqual, без правки которого все round-trip того файла молча роняют новое поле (класс Ruling 2 и Н-34, пятый раз за эпик). A-Т26-3 (Important, урок 609 применён сразу): у оси ПОЗИЦИЯ битов 5-7 и у оси независимость от битов 0-4 нет ни одного свидетеля — оба плановых теста ставят глубину при нулевых флагах, поэтому мутация сдвига на бит 4 переживает оба round-trip; конвенция файла на этот случай уже написана и уже исполнена Т20 для бита 4 (сырой байтовый пин плюс проверка невозмущения соседей). A-Т26-4: четвёртый тест плана дублирует ДВА живых пина SizeBytes, один из которых Т20 завёл ровно по этому поводу. A-Т26-5: ReplicateData прозрачен и правки не требует (копирует байт 7 целиком) — проверено телом, а не предположено. РУЛИНГИ 157-160: 157 глубина собирается из ДВУХ РАЗНОСТЕЙ, каждая внутри своего домена — (LocalTick минус ClientStateTick) плюс (NewestTick минус RenderTick); тождество раскладки через авторитетное состояние, вычитание нигде не пересекает границу доменов, обе величины проверены по коду включая FishNet 4.7.2 из PackageCache, при 80 мс RTT даёт 5-5.4 тика — ровно рабочую глубину спеки §3.6; мост часов app-7du2 НЕ требуется, потому что нужна длина интервала, а не адрес. 158 биты 5-7 получают свидетелей обоих видов по конвенции файла. 159 четвёртый тест плана не заводится (прецедент рулинга 148). 160 ReconcileCodecTests входит в скоуп записанным рулингом. ВОПРОС ПРО ProtocolVersion ЗАДАН ЯВНО И ЗАКРЫТ: бампа НЕТ. Довод тройной — правило файла говорит про смысл СУЩЕСТВУЮЩЕГО бита, а биты 5-7 значения не несли (Encode кладёт ноль, TryDecode их не читает); режим отказа противоположен всем четырём историческим бампам (там старый читатель ОТВЕРГАЛ блок, здесь он не отвергает ничего и лишь теряет отмотку); прецедент в том же файле — бит 4 занят Этапом 3 и в HISTORY про него нет записи вовсе. Закрывающий довод замерен: SimConfigHash фолдит RewindCapTicks и RewindPictureTicks с Т24, рукопожатие отвечает SimConfigMismatch, значит смешанной пары не существует по построению. Кода не написано, дерево ветки чистое.
Т26 КРАСНАЯ ФАЗА ЗАКРЫТА, предсказание сошлось целиком по ТРЁМ классам. Эвиденс task-26-red-evidence.md. Прогоны координатора лично, по очереди, один инстанс, собственный сторож. InputCodecTests: EXIT=2, 24 кейса, 19 зелёных, 5 красных, 25 c, error CS 0, Csc 8. ReconcileCodecTests: EXIT=2, 19 кейсов, 18 зелёных, 1 красный, 20 c. DeterminismTests: EXIT=2, 22 кейса, 18 зелёных, 4 красных, 165 c. testcasecount ВСЕГО проекта снят с вложенного сьюта ring-client-new во всех трёх прогонах и во всех трёх равен 1728 — счёт проверен машиной трижды без полного прогона. error CS ноль везде, то есть RED настоящий, а не ошибка компиляции. СЕМЬ КРАСНЫХ ТАСКА, все предсказаны до прогона по имени, месту падения и тексту, и все семь совпали: round-trip падает на итерации k=1 (при k=0 сравнение 0 с 0 проходит), семёрка на втором ассерте, независимость на шестом, FlagBits на шестом вызове AssertSingleFlagByte с Expected 32 But was 0, оба рефлексивных свипа множеством из девяти имён против восьми, кламп с Expected 6 But was 200. ГЛАВНОЕ ЧИСЛО: второй рефлексивный свип ReconcileCodecTests.ReplicateData_RoundTripsThroughInputCodec подтверждён МАШИНОЙ — план обещал три красных, handoff четыре, факт СЕМЬ; свипов по SimInput в дереве два, и второго не называют ни спека, ни план, ни handoff. ТРИ ЭТАЛОНА БИТ В БИТ ПРЕЖНИЕ: GoldenHash 6179821658852201134, MultiPlayer 1839905722486512695, Extraction 5806527071770847608; пины Expected не двигались. Отрицательная половина предсказания исполнена: сдвиг любого из трёх был бы находкой и не наступил, то есть довод про поле ввода вне StateHash фальсифицируем и не фальсифицирован. РУЛИНГ 161: тест клампа переезжает в DeterminismTests.cs к своей семье — блок стоит рядом с HostileInput_StateStaysFinite_AndDeterministic и весь про приведение враждебного значения в домен, сосед Sanitize_ClampsAimHeight написан буква в букву той же конструкцией, а InputCodecTests класс-доком ограничивает себя кодеком и до этого таска не строил SimulationWorld ни разу. Мой встречный довод про прецедент AimPoint_NearSanitizeClampBoundary проверен и отвергнут мной же: тот мира не строит. ЦЕНА НАЗВАНА: md5 DeterminismTests.cs 71279f0e9debdba7a247e18e89532e4c -> ed1be790a10849bfaf546424dfd55ce5, это НЕ перепин (три golden-литерала на месте, санкция Т34 цела, прецедент Т15), старое значение МЁРТВО и обязано быть заменено в handoff. ДВЕ ОШИБКИ КООРДИНАТОРА, ПОЙМАННЫЕ ИСПОЛНИТЕЛЕМ (правило 514/526, шестнадцатый наряд подряд): арифметика зелёных — я перенёс базу, не вычтя трёх покрасневших, верно 1718 а не 1721; и рулинг 157 не называл окно до первой реконсиляции, где ClientStateTick равен UNSET_TICK и первая скобка формулы даёт возраст процесса, а кламп превратил бы его в максимальную глубину на каждом выстреле — рулинг получил третью клаузу до единой строки продакшн-кода. Принято четыре расхождения: ConfigA вместо плановой фикстуры (у неё RewindCapTicks ноль, поэтому она различает проводное правило 7 читается как 6 от клампа по конфигу, а плановая пропустила бы второе молча; и виноват мой наряд, который противоречил сам себе), рулинг 161, плюс два дореформенных долга — сообщение байта 7 не называет бит 4 с Т20 и висячая ссылка на несуществующий тест в ReconcileCodecTests, оба чинит зелёная фаза.
Т26 ЗАКРЫТ КОДОМ, коммит 8b5aa8d, запушен и сверен живым ls-remote (8b5aa8d465a512a3f34172206470fb326581755b). Семь файлов, +392/-6, трейлера нет (Р436). Эвиденс task-26-green-evidence.md. ПОЛНЫЙ ПРОГОН НА ТОЧНЫХ БАЙТАХ: EXIT=2, 375 c, total 1728, зелёных 1725, красных РОВНО ТРИ, error CS 0. ЭТАЛОНЫ БИТ В БИТ ПРЕЖНИЕ: GoldenHash 6179821658852201134, MultiPlayer 1839905722486512695, Extraction 5806527071770847608; пины Expected не двигались, перепин остаётся один в Т34. Четыре фильтра до него: InputCodecTests 24/24 EXIT=0, ReconcileCodecTests 19/19 EXIT=0, PredictionParityTests 14/14 EXIT=0, DeterminismTests 22 кейса и ровно три красных. Зелёный паритет предсказания это исполнение Р34: прогон идёт через Decode(Encode(raw)), значит глубина пережила ПРОВОД, а не просто записалась в поле. МУТАЦИИ M46-M54, ДЕВЯТЬ: семь убиты названной жертвой на названном ассерте, две предсказаны выжившими и выжили. Откат сверен ПОСЛЕ ALLDONE, все три файла байт в байт равны базовым, маркеров ноль. Счёт мутаций эпика 149 + 9 = 158. M46 ГЛАВНАЯ: ветку насыщения нашёл ИСПОЛНИТЕЛЬ в собственном коде и назвал её выжившей ДО прогона — все присваивания RewindTicks в тестах давали 0..6, единственная двухсотка шла в санитайзер мимо кодека, поэтому подмена насыщения на маску пережила бы весь сьют. Рулинг 162 закрыл ветку одной строкой в уже правимом тесте, без нового Test и без сдвига total; замерено: M46 умирает единственной жертвой, 224 против 0. ТРЕТЬЕ ПОДТВЕРЖДЕНИЕ НЕПОДВИЖНОСТИ ЭТАЛОНОВ ПОЛУЧЕНО МУТАЦИЕЙ: под M50 (кламп снят целиком) три эталона напечатали ТЕ ЖЕ числа — если бы кламп доезжал до эволюции мира, его снятие сдвинуло бы их. Одно расхождение предсказания разобрано, а не сглажено: у M52 жертв три, а не две (удаление записи битов роняет и тест независимости), то есть мутация сильнее предсказанной — класс M1/M2/M3 Т2. ГЕЙТЫ: ГЕЙТ-КОДОГЕН ноль во всех четырёх ScriptAssemblies и в Ring.Networking.dll артефакта релизного сервера, плюс позитивный контроль (RewindTicks в Ring.Networking.dll встречается, значит прибор смотрит туда, куда надо); свипы прогнаны ВТОРОЙ раз перед коммитом — кириллица вне Assert ноль, британизмы ноль и без -i и с -i, запрещённая лексика ноль, UnityEngine в Simulation ноль, маркеры ноль, секрет-чек пуст; сцена 269 / 0241c714945a15bf1c68060f075209a4 не двигалась; Data не тронут, значит R-APPLY и R-IDEM не нужны; новых файлов нет, значит META и NUL-чек неприменимы. ЧЕТЫРЕ ОШИБКИ КООРДИНАТОРА ЗА ТАСК, ВСЕ ПОЙМАНЫ ИСПОЛНИТЕЛЕМ (514/526, шестнадцатый наряд подряд), и ДВЕ ИЗ НИХ БЛОКИРУЮЩИЕ: арифметика зелёных; рулинг 157 без окна UNSET_TICK; _nm.PredictionManager internal в пакете, то есть моя строка дала бы CS0122 и шаг не собрался бы; правка сторожа полей не была названа, без неё четвёртый красный. Плюс две неверные опоры: довод про перегрузку math.min (перегрузки byte-byte не существует, (int,int) выбралась бы и без каста) и подстрочный гейт-греп, дающий ложную тревогу на доковой ссылке — в гейт идёт форма по границе слова. ОПЕРАЦИОННАЯ НАХОДКА: nohup НЕ СПАСАЕТ от таймаута bash-тула, тул снимает всю группу процессов — прогон эталонов был убит на 165-й секунде и оставил зомби с mono-gdb в логе. Лечение проверено: setsid nohup с disown, своя сессия процессов тулу не подчиняется. Это дополнение к уроку 584, а не замена: там ломался инструмент, здесь область действия сигнала.
СЕССИЯ 76, КРУГ РЕВЬЮ Т26 (коммит 8b5aa8d, два ревьюера, Р444). Итог: Critical 1, Important 2, Minor 7, ЛОЖНЫХ 0; три находки ревьюеры сняли сами. Каждая проверена координатором лично, открытием файла. ⛔⛔ A-REV-1 (Critical) ПОДТВЕРЖДЕНА: PredictionManager.ClientStateTick присваивается ровно дважды (PredictionManager.cs:647 и :726), оба раза внутри ReconcileToStates, и :726 гасит его в UNSET_TICK безусловно перед выходом; наш обработчик висит на OnPreTick, который в теле TimeManager do{} поднимается на :726 СТРОГО РАНЬШЕ вызова ReconcileToStates на :739. Значит predictionLead тождественно 0, домен FishNet выпал, на провод уезжала константа InterpBufferTicks=3 вместо рабочих 5 тиков спеки §3.6 — и Т27 получил бы k_ввод=0 на каждом выстреле. Это дефект МОЕГО рулинга 157, причём второй: гард UNSET_TICK, добавленный на красной фазе, поломку ЗАМАСКИРОВАЛ, а не починил. Лечение в дереве уже есть: NetworkBehaviour.Prediction.cs:1468 ставит тик реконсиляции равным ClientStateTick для владельца, он защёлкнут в PlayerPredictionCore.LastReconciledTick, доступ через _controller.Core (уже используется в трёх местах бэкенда). Форма рулинга 157 верна, менять надо ИСТОЧНИК первой скобки. B-1: у клампа санитайзера нет свидетеля оси min-против-присваивания (единственная пара санитайзер+RewindTicks во всём дереве кормит 200 и ждёт кап). B-2: док метода называет ложную причину непокрытия — RenderClock и SnapshotQueue public sealed и строятся в EditMode руками (7 и 4 места), asmdef ссылается на Ring.Presentation.Net и FishNet.Runtime. A-REV-4 ПОДРЕЗАНА собственной командой ревьюера: он предсказал ноль тестовых строк на TicksFromSeconds(0.2f), а RewindTests.cs:16 пинит её прямо. Эвиденс — task-26-review-evidence.md.
КРУГ ПРАВОК Т26 ЗАКРЫТ — коммит 6a2a4d5 (11 файлов, +517/-105), запушен и сверен живым ls-remote (6a2a4d57182755523ceb0ccec4bd7d83bec06ff9). Девять рулингов 163-171. Поведенческих правок четыре: (163) первая скобка глубины берёт PlayerPredictionCore.LastReconciledTick вместо мёртвого ClientStateTick — на владеющем клиенте это тот же тик, защёлкнутый пока он жив, закрыты обе двери, удалённая и локальная; (165) потолок замера стал проводным InputCodec.MaxRewindTicksOnWire, балансный Arena.RewindCapTicks ушёл из презентации — второй дом политики снят; (166) гард часов RenderClock.Placed вместо Started, закрыто окно между стартом и постановкой; (170) HasRenderTick дев-панели туда же — поле заводили ровно чтобы панель не печатала render 0, а окно этот симптом оставляло. Покрытие: (164) арифметика вынесена в чистый RewindDepthMeter рядом с бэкендом по прецеденту PlayerPredictionCore, восемь свидетелей в новом RewindDepthTests; (167) кламп санитайзера получил свидетеля оси min-против-присваивания, отдельным тестом. Правдивость: (168) шесть доковых правок по B-3..B-6 и A-REV-4; (169) ложный класс-док FramePresenceTests про ссылку asmdef; (171) устаревшее verbatim-transfer в доке SimInputSanitizer. КРАСНАЯ ФАЗА: RewindDepthTests 8 кейсов 7 красных, DeterminismTests 23/3 — предсказание сошлось обеими половинами, error CS 0. ЗЕЛЁНАЯ: 8/8 EXIT 0. ПОЛНЫЙ ПРОГОН 370 c: 1737 / 1734 зелёных / красных ровно три — эталоны, But was прежние 6179821658852201134, 1839905722486512695, 5806527071770847608. МУТАЦИИ M55-M65: 11 из 11 сошлись включая числа 253/255; 8 убиты названной жертвой, 3 предсказаны выжившими и выжили. M64 вкручивает дефект A-REV-1 обратно и остаётся зелёной — измеренное доказательство, что вынос закрывает арифметику, а не проводку; свидетель проводки только лаг-гейт Ф4. M62 доказала рулинг 167: подмена min на присваивание убивает ровно новый тест, 2 -> 6. Счёт мутаций эпика 158+11=169, рулингов 162+9=171. Гейты чисты: сцена 269/0241c714945a15bf1c68060f075209a4, ProtocolVersion 4, кодоген 0 ошибок, секретов нет, .meta от Unity (доказано mtime), русской прозы и британизмов в диффе нет. Эвиденс — task-26-fixround-{a,b}-brief.md, task-26-fixround-red-evidence.md, task-26-fixround-green-evidence.md.
СЕССИЯ 76 ЗАКРЫТА. Handoff обновлён посекционно: 1171 -> 1238 строк, тип НЕСТАНДАРТНАЯ, скоуп сессии 77 задан владельцем — полное выполнение Т27 (план 5098-5262). Гейт урока 596 исполнен: md5 защищённой секции app-zuo сверен до и после — 3b0a157f5dde8dd48d5d63e3acabfae0 в обеих точках. ⚠ Считать его надо ОТ ЗАГОЛОВКА секции до конца файла, номера строк плывут. ОШИБКА ПРАВКИ, пойманная и исправленная: в §10 якорь секции был процитирован ДОСЛОВНО, и на следующем проходе str.index нашёл цитату вместо заголовка — хвост файла задвоился (1201 -> 1600 строк, два §11 и два §12). Поймано сверкой числа строк сразу после прохода; вылечено обрезкой по rindex и снятием самой ловушки. Тот же класс, что урок 617. ЛЕДГЕР дописан ДО handoff-а (урок 572): progress.md 9969 -> 10467 строк, уроки 614-619. Состояние на закрытие: main 59a7dea, ветка 6a2a4d5, обе сверены живым ls-remote, оба дерева чистые, стеши пусты, Unity не запущен, lockfile отсутствует, маркеров мутаций ноль, мусора в корне нет. Тестов 1737 / 1734 зелёных / три эталона красны намеренно; md5 DeterminismTests.cs = dbd00b04d6ef17d470fd80e5aab38fb7. ⚠ ДЛЯ Т27 ГЛАВНОЕ: план его числа снимал до Ф2 (total=1668 и PASS 10/10 мертвы, дерево несёт 1737 и RewindTests 16, ждать 1742 и 21); Т27 трогает Simulation, но golden-сценарии RewindTicks не выставляют, поэтому три эталона двигаться НЕ должны — записать это ДО прогона как отрицательную половину предсказания, сдвиг будет НАХОДКОЙ. app-3jxk (круг ревью Т14+Т23) назначен ПЕРЕД Т28, то есть следующим после Т27.


---

# ВЫГРУЗКА 2026-09-01 (сессия 80) — заметки, накопившиеся после выгрузки 2026-08-30

ЧЕМ КОНЧАЛАСЬ: фаза Ф3 прошла ТРИ таска из одиннадцати (Т24, Т25, Т26), у каждого круг ревью и круг правок. Т24 — PositionHistory и постоянный слот тела (8b0874e + b556880, рулинги 123-142). Т25 — история в хеше, сохранении и конце тика (67ef01b + 1500a76, рулинги 143-156). Т26 — глубина отмотки в трёх свободных битах ввода (8b5aa8d + 6a2a4d5, рулинги 157-171); его круг ревью нашёл Critical В РУЛИНГЕ КООРДИНАТОРА: первая скобка формулы глубины была тождественно нулём, и на провод уезжала конфигурационная константа вместо измерения. Полный прогон на байтах 6a2a4d5: 1737 тестов, 1734 зелёных, три красных — эталоны детерминизма, намеренно красные до Т34. Мутаций эпика 169, рулингов 171.
ФРОНТ: Т27 (k_ввод двигает снаряд догоняющими шагами) — скоуп сессии 77, задан владельцем. Дальше Т28-Т34, гейт фазы Ф3, веха В3. app-3jxk (круг ревью Т14 плюс Т23) назначен ПЕРЕД Т28.
ПРАВИЛО ДЛЯ СЛЕДУЮЩИХ СЕССИЙ: ноты снова принимаются — пиши сюда. Когда колонка снова подойдёт к 50 КБ, повтори приём: выгрузить дословно в docs/history/, закоммитить, заменить колонку указателем. Порядок обязателен — сперва история в git, потом правка трекера.
СЕССИЯ 77, старт. Сверка §10 сошлась целиком: main dbec49b, ветка 6a2a4d5, обе сверены живым ls-remote, оба дерева чистые (0 строк), stash пуст, worktree два, md5 DeterminismTests.cs dbd00b04d6ef17d470fd80e5aab38fb7, MUTATED/ABLATED ноль, сцена 269 / 0241c714945a15bf1c68060f075209a4 (с sort), Unity не запущен, lockfile нет, открытых PR нет. ListAgents: параллельной сессии трека нет. НАХОДКА СТАРТА: колонка notes несла 54 639 байт, а не обещанные handoff-ом ~35 КБ, то есть порог архивации 50 КБ был перейдён; история вынесена в docs/history/app-wpei-notes.md (коммит 1cfed28), колонка сжата до 2 836 байт (коммит f0b0095). СКОУП ВЛАДЕЛЬЦА: полное выполнение Т27, план 5098-5263 (адрес сверен лично, Т28 начинается на 5264).
АУДИТ Т27 (лично, приём 543, с диффа коммитов; файл task-27-audit.md). Т26 не тронул НИ ОДНОГО дома Т27 — проверено git diff --stat 1500a76..6a2a4d5, вывод пуст; адреса снимались с диффа всего эпика 3cb379d..6a2a4d5. АДРЕСА: три из четырёх уехали, SlideTimer цели с 430 на 711 (+281), своп мобов с 1607 на 1953 (+346), конец тела цикла с 325 на 546 (+221); WeaponSystem.cs эпиком не тронут ни разу. ГРАНИЦА Т27/Т28 ЗАМЕРЕНА ГРЕПОМ: RewindTicks читают в Simulation ровно две строки (объявление и кламп санитайзера), RewindPictureTicks не читает никто — правило деления написано доком ArenaConfig 442-443 и НЕ написано кодом, Т27 его первый автор. ПЯТЬ НАХОДОК. A-Т27-1 Critical: своп-удаление — снаряд, погибший на догоне, оставляет слот за границей живого набора, и непрерванный догон эмитит второй конец одного раунда и затирает соседний ЖИВОЙ снаряд; план случая не называет. A-Т27-2 Important: тест порядка эмитов по плану до догона не доходит (план сам зовёт его сторожем), и число плана 0.78 м неверно — дуло стоит на MuzzleOffset плюс overshoot, то есть 1.7667 м, значит фикстура рождает снаряд ВНУТРИ препятствия на 0.267 м. A-Т27-3 Important: k=0 у моба исполняется не правилом, а тем, что мобы спавнят напрямую через MobAiSystem 384, минуя WeaponSystem — тест 44б сторож по построению. A-Т27-4 Important: вырожденный случай двух сборщиков с разными k назван спекой и планом поимённо и не имеет ни одного теста. A-Т27-5 Minor: тест 40 планом не написан, а его мутация назначена. РУЛИНГИ 172-177: функция шага возвращает признак остался-в-слоте и догон на нём прерывается; правило деления получает отдельный дом Simulation/Combat/RewindSplit.cs по прецеденту Spread.cs; фикстура стены переставляется на 4.5 м и строится на Quiet(); заводится тест двух сборщиков; 44б объявляется сторожем и рядом со спавном моба пишется правило. ЧИСЛА ДВУХ ТЕСТОВ ПЛАНА ПРОВЕРЕНЫ И СОШЛИСЬ: разница 2 шага при допуске 0.05 против косинусной потери 7.9e-4; Ttl 1.3666667 при допуске 1e-3.
Т27 ЗАКРЫТ КОДОМ — коммит c3b5414, шесть файлов, +1187/-462, запушен и сверен живым ls-remote. ПОЛНЫЙ ПРОГОН на точных байтах коммита: EXIT=2, 370 c, error CS 0, testcasecount 1744, зелёных 1741, красных РОВНО ТРИ — три эталона, и все три БИТ В БИТ прежние (6179821658852201134 / 1839905722486512695 / 5806527071770847608). RewindTests 16 -> 23. Отрицательная половина предсказания (580) исполнена и была нетривиальной: Т27 трогает Simulation, но генератор DeterminismTests.Scripted RewindTicks не выставляет — проверено ТЕЛОМ, не записью handoff-а, значит k=0 и догона нет. РУЛИНГИ 172-180. 172: функция шага отвечает остался-ли-снаряд-в-слоте и догон на этом прерывается — довод своп-удаление, слот погибшего на догоне уходит за границу живого набора. 173: правило деления получило дом Simulation/Combat/RewindSplit.cs по прецеденту Spread.cs, док называет ОБЕ половины урока 616. 177: у моба глубина нулевая записанным правилом, мобий путь идёт мимо WeaponSystem. 178: наружу торчит CatchUp, а не голый шаг. 179: фикстура стены переставлена с 4.5 на 3.6 м. 180: охрана отказанного спавна получила свидетеля. МУТАЦИЙ ОДИННАДЦАТЬ (M66-M76), выживших НОЛЬ, счёт эпика 180. M67 восстановила красную фазу БИТ В БИТ — доказано, что GREEN добавил ровно одно. M75 — главная: снятие прерывания догона убивает тест стены ИСКЛЮЧЕНИЕМ, и на прежней фикстуре она выжила бы. ДВЕ МОИ МУТАЦИИ БЫЛИ НАПИСАНЫ НЕВЕРНО и перегнаны: M71 использовала короткозамкнутый оператор и потому не вызывала шаг вовсе, M73 брала индекс за границей. Урок: сухая проверка якорей ловит ненайденную строку и НЕ ловит мёртвый код за найденной. ДВАЖДЫ ИСПОЛНИТЕЛИ ПОЙМАЛИ КООРДИНАТОРА БЛОКИРУЮЩЕ: фикстура ждала события конца раунда, а базовый конфиг разрешает рикошет, который возвращается ДО эмита (тест был бы красным навсегда); и фикстура клала контакт на ПОСЛЕДНИЙ догоняющий шаг, из-за чего рулинг 172 остался бы без свидетеля. Оба раза находка верна, а лечение исполнителя — нет. ГЕЙТЫ: провод не тронут ни строкой (ProtocolVersion 4), Data и сцена не тронуты (269 / 0241c714945a15bf1c68060f075209a4), свипы дважды чисты, два modelled в диффе — переехавшие строки из BASE, долг app-215e; .meta нового файла сгенерирована Unity на прогоне координатора (доказано mtime), GUID в дереве ровно один.
КРУГ РЕВЬЮ Т27 — CRITICAL 1, IMPORTANT 7, MINOR 6, ЛОЖНЫХ 0. Два ревьюера, разные углы (Р444). Каждая находка перепроверена координатором ЛИЧНО открытием файла. Одну свою ревьюер A снял сам с основанием по коду (RewindPictureTicks=0 недосягаем: NetInvariants правило 11 гейтит старт сервера). B-1 CRITICAL: RewindSplit пришёл в дерево БЕЗ ЕДИНОГО ТЕСТА (греп по client/Assets/Tests даёт ноль), при том что док класса цитирует урок 616 и объявляет арифметику деления под свидетелем — то есть я процитировал правило и нарушил его в том же файле, назвав покрытой половину, у которой покрытия нет, и непокрытой ту, у которой оно есть. И косвенно закрыть min нельзя в принципе: мутант min -> константа даёт отрицательный steps, а цикл for не исполняется ровно так же как при нуле — эквивалентный мутант через провод потребителя. A-1 IMPORTANT: догон убивает В ОРУЖЕЙНОЙ ФАЗЕ, а она читает Alive живьём на каждой итерации — значит снаряд сборщика 0 может убить сборщика 1 и лишить его выстрела в этом тике, обратное невозможно; новая асимметрия по номеру слота, которой в оружейной фазе не было (до Т27 убийства жили только в ProjectileSystem, идущем ПОСЛЕ). A-2 IMPORTANT: мой довод наряда догон идёт по старым позициям потому что он в прошлом ЛОЖЕН по коду — догон видит сборщиков в позициях ЭТОГО тика и мобов прошлого, то есть гибрид, и для шага изображающего тик T минус k эти позиции на k тиков в БУДУЩЕМ. Док самого кода при этом честен. Следствие названо ревьюером и записывается: между Т27 и Т28 лаг является ПРЕИМУЩЕСТВОМ. A-5 и B-4 IMPORTANT: единственный свидетель RULING 172 прибит к литералу 3.6 и слепнет БЕСШУМНО с обеих сторон окна 21.6 < ProjectileSpeed < 43.2, а Т23 поднял потолок скорости до 300 намеренно. B-2 и A-3 и A-4 IMPORTANT: из пяти сайтов stillInSlot=false свидетеля имеет ОДИН (стена); телесный конец, пол, истечение и оба исхода контакт-был-снаряд-остался (рикошет, пробитие) на догоне не свидетельствуются ничем, а рикошет на догоне ЖИВ в отгружаемом балансе. B-3 IMPORTANT: комментарий this loop is the only caller there is today написан ЭТИМ ЖЕ коммитом и лжёт — вызовов два, и стоит он ровно над тем, который возврат выбрасывает. A-6 MINOR, УСИЛЕН МНОЙ ЧИСЛОМ: спека оправдывает догон словами 1-2 тика и таблицей 1.75-3.5 м, а деление даёт 0..3; на ОТГРУЖЕННЫХ числах (ProjectileSpeed 52.5, кап 6, картинка 3) максимум хитскан-полосы 5.25 м, а не 3.5 — то есть кап рассчитан на RTT 100 где полоса в полтора раза больше обещанной таблицей. Это число владелец принимал решением Н24/Р407, запись идёт в decision log спеки. A-7 MINOR: провод везёт высоту и скорость из СНИМКА конца тика, а позицию из СОБЫТИЯ (с дула) — Т27 развёл их с одного шага до четырёх; чинится Т32 по Р408, но шов нигде не записан. Прочие Minor: недостижимый второй ассерт теста 1, неверно обоснованная пара личности теста 6, док CatchUp говорит re-read from w про dt который берётся из статики, абзац DOMAIN не называет NetInvariants 11 как настоящие ворота нижней границы.
КРУГ ПРАВОК ПО РЕВЬЮ Т27 ЗАКРЫТ — коммит 6b8535b, четыре файла, +509/-31, запушен и сверен живым ls-remote. ПОЛНЫЙ ПРОГОН на точных байтах: EXIT=2, 371 c, error CS 0, testcasecount 1749, зелёных 1746, красных РОВНО ТРИ, три эталона БИТ В БИТ прежние, RewindTests 28/28. Тестов 1744 -> 1749 (RewindTests 23 -> 28). ПОВЕДЕНИЕ ПРОДАКШН-КОДА КРУГОМ НЕ ИЗМЕНЕНО НИ СТРОКОЙ — доказано машиной: дифф по Simulation/Combat с вычетом комментариев пуст. РУЛИНГИ 181-190. 181 закрыл Critical: RewindSplit получил три прямых теста, и мутация M78 (та самая, эквивалентная через провод потребителя) умирает на двух из них. 185 сделал слепоту свидетеля громкой: мутация M82 (скорость фикстуры 35 -> 52.5, то есть на отгруженное балансное число) краснит четыре теста, три на самой премиссе. 186 завёл свидетелей телесному концу догона и рикошету на догоне; пробитию свидетель НЕ заводится намеренно — на отгруженных числах пирс отказывает всем телам, и фикстура с поднятым порогом свидетельствовала бы правило Т20, а не Т27. 188: спека получила §6f — её таблица обещает хитскан-полосу 1.75-3.5 м, а деление на отгруженных числах (ProjectileSpeed 52.5, кап 6, картинка 3) даёт максимум 5.25 м; 3.5 это типичный случай при RTT 80, а не граница, на которую сам кап рассчитан. Код исполняет формулу буквально, расходятся два места САМОЙ СПЕКИ; правка §3.6 — решение владельца, оба варианта названы в §6f. 189: заведена app-56kx (шов провода перед Т32, discovered-from). МУТАЦИЙ ШЕСТЬ (M77-M82), выживших НОЛЬ; счёт эпика 186, рулингов 190. ДВАДЦАТЫЙ НАРЯД ПОДРЯД ПОЙМАЛ КООРДИНАТОРА: наряд писал шиппинговый MaxRicochets = 1, а и C#-дефолт, и ассет несут 2 — единица есть намеренное отклонение фикстуры, записанное в TestConfigs и охраняемое ConfigTests (Ruling 94); исполнитель написал в док правду, а не букву наряда. ГЕЙТЫ: провод не тронут (ProtocolVersion 4), Data и сцена не тронуты (269 / 0241c714945a15bf1c68060f075209a4), новых файлов ноль, свипы чисты, секретов нет.
Сессия 79, аудит Т28 (task-28-audit.md, лично, с диффа, тела читаны). Адреса: AcceptCandidate :834 (handoff обещал :831), вызов Resolve :1053 (:1043) - обе цитаты мертвы. НАХОДКИ: A-1 Т28 НЕ проводной таск - замерено списком генерированных сериализаторов Ring.Networking.dll: из типов симуляции у кодогена есть только PlayerState, у ProjectileState сериализатора НЕТ (grep Ricochets в Networking.dll = 0 против HistorySlot = 1); критерий правила 1 даёт НЕТ, ProtocolVersion не двигается. A-2 формула плана currentTick-RewindLeft и фраза спеки T-k+i расходятся только при k=2,3,4 (до 0.5 м по сборщику); на расчётных точках спеки RTT 80/100 и на тике рождения тождественны. A-3 PosAt не умеет сказать, что ответила вырожденной веткой - флаги Sliding/Invulnerable в ней молча нулевые. A-4 у отмотанной неуязвимости нет дома в AcceptCandidate - живой гейт в ветке HitPlayer и в DamagePlayer. A-5 широкая фаза гейтит сборщика живым Alive. A-6 параметр historyTick становится выводимым из RewindLeft. ЗОНД (разведка, 606): все три эталона воспроизведены бит в бит; живых снарядов на финальном тике - соло 0, мульти 18, извлечение 17.
Т28 красная фаза закрыта, предсказание сошлось ОБЕИМИ половинами. RewindTests: EXIT=2, 30 c, error CS 0, Csc 8, вложенный testcasecount 1764 (1755+9), 37 кейсов, 31 зелёный, 6 красных - ровно названные шесть и на названных ассертах (But was False / 1000000 / True / True / 88.0 / 100.0; 88.0 = MaxHp 100 минус Damage 12 до цифры). Четыре сторожа зелены. WorldLifecycleTests: 10 кейсов, 1 красный - рефлексивный свип, сообщение ProjectileState.RewindLeft не в хеше (квитанция дисциплины, как у Ricochets на Т19). DeterminismTests: 23 кейса, три эталона красны намеренно и БИТ В БИТ прежние - структура мир не двигает. ИСПОЛНИТЕЛЬ ПОЙМАЛ КООРДИНАТОРА ЧЕТЫРЕЖДЫ (двадцать третий наряд): держаный триггер красил бы три фикстуры на ВЕРНОМ коде (FireCooldown декрементится ДО while, второй раунд на 4-м тике); круг контакта в наряде посчитан с чейзеровым радиусом вместо сборщикова (6.38 вместо 6.43, запас 3 мм); заморозку моба нельзя пробировать после попадания (DamageMob толкает Vel, при Accel=0 затухание не съедает); PosAt зовут пять тестов, а не три. Эвиденс - task-28-red-evidence.md.
Т28 ЗАКРЫТ КОДОМ - коммит c9db8c0, девять файлов, +1632/-136, запушен и сверен живым ls-remote. ПОЛНЫЙ ПРОГОН НА ТОЧНЫХ БАЙТАХ: EXIT=2, 410 c, error CS 0, вложенный testcasecount 1767, зелёных 1764, красных РОВНО ТРИ. ЭТАЛОНЫ - ПРЕДСКАЗАНИЕ 3 ИЗ 3: соло But was 6179821658852201134 (БИТ В БИТ прежний, предсказано зондом - ноль живых снарядов на финальном тике), мульти 1839905722486512695 -> 12948336956096453079, извлечение 5806527071770847608 -> 12266730622305861208. Перепин НЕ потрачен. МУТАЦИИ M89-M108: 20 заходов, 18 убиты названной жертвой, откат сверен md5 после ALLDONE. M107 выжила - эквивалентна по построению (вырожденную запись боевой путь не читает, два ранних возврата). M108 выжила на слабом ассерте, сторож усилен до абсциссы контакта, мутация перегнана и убита. ДЫРА, НАЙДЕННАЯ КООРДИНАТОРОМ ДО ЦИКЛА: отмотка позиции проверялась только на мобах, у сборщика свидетеля не было - заведён, M104 убивает ровно его. ГЕЙТЫ: свипы дважды (кириллица вне Assert 0 - поймана и снята русская цитата плана в комментарии, британизмы 0 с -i, TODO 0), секреты 0, новых файлов 0, сцена 269/0241c714945a15bf1c68060f075209a4 не двигалась, UnityEngine в Simulation 0, Data не тронут, ProtocolVersion 4. ГЕЙТ-КОДОГЕН по рулингу 202: Т28 НЕ проводной таск (у ProjectileState нет генерированного сериализатора), дешёвая половина прогнана - 0 во всех четырёх сборках, codegen error 0.
КРУГ РЕВЬЮ Т28 И КРУГ ПРАВОК ЗАКРЫТЫ. Ревью двумя ревьюерами: Critical 0, Important 5, Minor 11, ложных 0; оба вердикта принять с доработкой, ревертить нечего. КРУГ ПОПРАВИЛ КООРДИНАТОРА ТРИЖДЫ: я объявил выжившую M107 эквивалентной, а это ДЫРА - она портит ответ живой ветки (historyTick == 0 при выстреле на тике, равном глубине; строки тика 0 нет никогда), и ветка исполняется двумя существующими фикстурами; основание нечувствительности фикстур к двусмысленности 203 я принял шире, чем оно есть; скорость сборщика цитировал из C#-дефолта 7, тогда как отгруженный HeroConfig.asset несёт 7.5. Круг правок: коммит af53719, пять файлов, +717/-52, ПОВЕДЕНИЕ НЕ ТРОНУТО (строк кода вне комментариев в Scripts - ноль, замерено фильтром диффа). Три новые оси со свидетелями: мобий исторический Alive (рулинг 214), половина картинки на спавне и трата счётчика догоняющим шагом одним числом (215), вырожденный ответ без строки (220). Плюс пара неуязвимости закрепляет конец раунда (216), три негативные фикстуры - что выстрел был (217), одиннадцать доков перестали лгать (218). МУТАЦИИ M107b/M109/M110 - три из трёх, каждая убила ровно своего свидетеля, включая ту, что раньше переживала весь набор. ПОЛНЫЙ ПРОГОН: EXIT=2, 370 c, error CS 0, testcasecount 1770, зелёных 1767, красных ровно три - эталоны БИТ В БИТ те же, что у c9db8c0. Гейты: свипы чисты, сцена не двигалась, кодоген 0 во всех четырёх сборках, ProtocolVersion 4. Эвиденс - task-28-review-evidence.md.
СЕССИЯ 79 ЗАКРЫТА по команде владельца. Т28 закрыт целиком: код c9db8c0, §6g спеки 00cb43e, круг правок af53719; main b1ceb2d. Обе ветки запушены и сверены живым ls-remote, оба дерева чистые, стешей нет, процессов Unity нет, маркеров мутаций ноль. Тестов 1770 / 1767 зелёных / три эталона красны намеренно; мутаций эпика 215; рулингов 220; уроков 108-636. Handoff обновлён на сессию 80, скоуп Т29 (санитарная проверка заявленной глубины, план 5530-5646) - первый таск эпика вне симуляции: создаёт файл (ГЕЙТ-META и NUL-чек), трогает балансный ассет (R-APPLY, R-IDEM, сверка сцены) и работает в Networking/Server. Защищённая секция параллельного трека сверена md5 до и после: 3b0a157f5dde8dd48d5d63e3acabfae0, совпала. ДВА РАСХОЖДЕНИЯ СПЕКИ ЖДУТ ВЛАДЕЛЬЦА: §6f (хитскан-полоса 5.25 м против 3.5) и §6g (отмотанные часы неравномерны - спрашиваются T-3, T-1, T+1). Перепин эталонов НЕ потрачен.
СЕССИЯ 80 СТАРТ: сверка сошлась целиком — main 58c1a17, ветка af53719, обе сверены живым ls-remote, оба дерева чистые (0 строк), stash пуст, worktree два, Unity не запущен, lockfile нет, MUTATED ноль, md5 DeterminismTests.cs dbd00b04d6ef17d470fd80e5aab38fb7, сцена 269 / 0241c714945a15bf1c68060f075209a4 с sort, счётчики тестов по классам сошлись с handoff (RewindTests 43, RewindDepthTests 8, DeterminismTests 23, NetInvariantsTests 23, ConfigTests 79). Колонка notes на старте 28306 Б при пороге 50 КБ. Скоуп — Т29, задан владельцем. АУДИТ Т29 ЛИЧНО (543, тела): task-29-audit.md, семь находок, две несущие. A-Т29-1 CRITICAL: TimeManager.RoundTripTime на выделенном сервере тождественно ноль — замерено по запиненному FishNet 4.7.2 (единственный писатель TimeManager.cs:611 внутри ModifyPing, единственный вызывающий ClientManager.cs:666 ParsePingPong, TrySendPing под гардом isClient на TimeManager.cs:760; ServerBootstrap поднимает только ServerManager.StartConnection). Подушевого RTT у сервера в 4.7.2 нет: NetworkConnection.PingPong считает только частоту, Tugboat подушевого пинга не отдаёт, LiteNetLib NetPeer.RoundTripTime закрыт приватным ServerSocket.GetNetPeer. Следствие: формула вырождается в min(claimed, 5, 6), то есть каждый игрок с RTT от 100 мс молча теряет один тик отмотки — поведенчески это вариант б развилки §6f, которую владелец ещё не решал. Тот же класс, что дефект T26 fix-round (A-REV-1): шапка RewindDepthTests записала его дословно — liveness is a property of the WIRING, not of the formula. Развилка по скоупу вынесена владельцу. A-Т29-2 CRITICAL для красной фазы: четвёртый тест плана сегодня ЗЕЛЁНЫЙ — правило #11 живёт с Т24 (NetInvariants.cs:390) и имеет ТРИ свидетеля; красных будет ДВА, а не три; прецедент отказа — RULING 159. Адресный дрейф: план :951 → реально :1222, план NetworkSimBackend :900 → :903, Validate :175 жив.
РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-09-01: Т29 РАЗДЕЛЁН НА ДВА ТАСКА — app-j4j8 (Т29а, сервер измеряет круг) и app-276d (Т29б, трим на замере), app-276d заблокирована app-j4j8, обе parent-child к app-wpei. Причина — A-Т29-1: источник оценки, который называет план и спека §3.6 (TimeManager.RoundTripTime), у выделенного сервера тождественно ноль. Владелец отверг вариант отгрузить арифметику без проводки как срезанный угол (правило 1 AGENT.md §4) и выбрал полное решение, разделённое на два таска: не влезет в одну сессию. ОТДЕЛЬНО: спека §3.7 запрещает рост раскладки ввода (Н4, InputCodec.SizeBytes остаётся 8), поэтому форма замера — не девятый байт ввода, а отдельный broadcast по образцу SpectateRequestNet. Расхождение спеки (§3.6 называет несуществующий источник) идёт записью в decision log спеки §6h на Т29а; правку §3.6 делает владелец. ПАРАЛЛЕЛЬНО по прямой команде владельца пересобраны ДВЕ клиентские цели из af53719: linux-client (EXIT=0, 100 c) и windows-client (EXIT=0, 101 c), error CS 0 в обеих; сборки 1280593 в этих целях затёрты, остальные четыре цели по-прежнему 1280593. Проверено ПО САМИМ СБОРКАМ: PositionHistory 4, HistorySlot 1, RewindSplit 3, CatchUp 1, RewindLeft 1 в Ring.Simulation.dll обеих; отрицательный контроль на нетронутом linux-client-dev даёт все нули. Сборка оставила побочный след — Unity переписал m_PrefilterDBufferMRT3 в client/Assets/Settings/PC_RPAsset.asset, откачено, дерево 0 строк, сцена 269 / 0241c714945a15bf1c68060f075209a4.
Т29 КРАСНАЯ ФАЗА ЗАКРЫТА, предсказание сошлось обеими половинами. Фильтр RewindSanityTests: EXIT=2, 91 c, error CS 0, Csc 7, total 4, passed 1, failed 3 — ровно предсказанные три на предсказанных ассертах и числах (5/6, 4/6, 6/7), четвёртый зелёный сторож. Отрицательная половина прогнана ОТДЕЛЬНЫМ фильтром DeterminismTests: EXIT=2, 200 c, 23/20/3, все три эталона БИТ В БИТ прежние — соло 6179821658852201134, мульти 12948336956096453079, извлечение 12266730622305861208; перепин Т34 не потрачен. Рулинги таска 221-227 записаны ДО наряда: 221 четвёртый тест плана не заводится (правило #11 живёт с Т24 и имеет три свидетеля, прецедент RULING 159); 222 его место занял свидетель капа AnEstimateAboveTheCap_IsStillBoundedByIt, без него мутация убрать capTicks из min пережила бы весь набор; 223 боевой путь исполняет спеку дословно, факт нулевого RoundTripTime записывается доком, §6h и пунктом гейта Ф4, изобретать серверный замер ЗАПРЕЩЕНО; 224 лог урезания по фронту, прецедент SpectatePolicy.ShouldLogRefusal; 225 маркер-ключ четырьмя правками; 226 Range у RewindSanityTicks - подсказка инспектора, клампа не заводить; 227 шов остаётся на MatchServer, прецедент - шесть таких же швов в файле. ИСПОЛНИТЕЛЬ ПОЙМАЛ КООРДИНАТОРА ПЯТЬ РАЗ, все проверены лично открытием файла: F1 швы MatchServer internal static, а не public, и тесты видят их через InternalsVisibleTo — мой наряд и рулинг 227 говорили public, исправлено; F2 ярлык как тики даёт 40, а не 1200 (1200 — это выброшенный множитель 0.001f), и план был прав а я нет; F3 независимо подтвердил рулинг 221; F4 энкодер насыщает на 7, до 6 доводит декодер, мой наряд склеил два числа; F5 дословная цитата TiltSystem — a battle path calling a method named ForTest is the exact defect — против имени из плана, но контрпример WaveSystem.CountForTest в дереве тоже есть, имя из апрувнутого плана НЕ меняю, находка выносится кругу ревью.
Т29 ЗАКРЫТ КОДОМ. Коммиты 437f0ea (feat, семь файлов, +406/-8) и 583accc (docs, §6h спеки, +64). ЗЕЛЁНАЯ ФАЗА: фильтр RewindSanityTests EXIT=0, 30 c, 4/4. МУТАЦИИ M111-M116 — ШЕСТЬ ИЗ ШЕСТИ убиты названной заранее жертвой; M114 (убрать capTicks из min) убила РОВНО ОДИН тест — тот, который завёл рулинг 222, и без него прошла бы весь набор, то есть дыра была настоящей. Сухая проверка якорей поймала неуникальный якорь ДО цикла (roundTripMs * 0.001f * 0.5f встречался дважды — в доке и в коде), удлинён префиксом SimulationWorld. Мутация M117 (лог каждый тик вместо фронта) НЕ гонялась и причина названа: боевой путь OnPostTick не исполняет ни один тест, класс по построению не юнит-тестируется, это мутация без свидетеля по объявленной заранее границе рулинга 224, а не дыра. Откат копией, md5 после ALLDONE совпал с базой, маркеров мутаций ноль. R-APPLY и R-IDEM: оба Apply EXIT=0, md5 NetConfig.asset после второго совпал с первым (b43f49147e3f75686aeeb0a67750e7b4), дифф ассета РОВНО ОДНА строка RewindSanityTicks: 2, EntityFadeTicks на месте, сцена 269 / 0241c714945a15bf1c68060f075209a4 не двинулась. ПОЛНЫЙ ПРОГОН НА БАЙТАХ КОММИТА: EXIT=2, 440 c, error CS 0, testcasecount вложенного ring-client-new 1774, зелёных 1771, красных ровно три — все три эталона БИТ В БИТ прежние, предсказание три из трёх, перепин Т34 не потрачен. Предсказание счёта 1774/1771/3 сошлось до цифры. ГЕЙТЫ: свипы дважды чисты, META (новый файл несёт .meta от Unity), NUL-чек, R-APPLY, R-IDEM, сцена, кодоген 0 во всех четырёх ScriptAssemblies и codegen error 0 — половина артефакт релизного сервера НЕ гонится, потому что по верному критерию RULING 202 таск НЕ проводной: NetConfig это ScriptableObject, кодоген его не сериализует. ProtocolVersion 4 не двигалась. ИСПОЛНИТЕЛЬ ЗЕЛЁНОЙ ФАЗЫ ПОЙМАЛ КООРДИНАТОРА СЕМЬ РАЗ: F1 эталон теста 3 и аргумент формулы — разные float (0.03999999910593033 против 0.04000000283122063), сходятся только удалённостью от границы округления; F2 конвенция NetConfig надгробий не оставляет, абзац маркера уезжает целиком — исполнена конвенция файла, а не буква наряда; F3 зона Networking не знает System.Math (0 против 44 math.min), и SimInputSanitizer применяет ту же идиому к ЭТОМУ ЖЕ полю — исправлено на math.min; F4 кап применяется ДВАЖДЫ (арена в SimInputSanitizer и заслуженность здесь), не-дублирование названо абзацем в коде; F5 RoundTripTime по собственному доку пакета includes latency from the tick rate, то есть даже на живом клиенте оценка консервативно завышена; F6 наряд вернул мёртвый путь Editor/ вместо Scripts/Editor/; F7 между Gather и TickAll не пусто — два цикла, ни один не читает RewindTicks. Все семь проверены координатором лично открытием файла. Эвиденс — task-29-green-evidence.md.
КРУГ РЕВЬЮ Т29 И КРУГ ПРАВОК ЗАКРЫТЫ. Ревью двумя ревьюерами: Critical 0, Important 7, Minor 8, ложных 0; оба вердикта принять с доработкой, ревертить нечего. ТРИ НАХОДКИ РЕВЬЮЕРЫ СДЕЛАЛИ НЕЗАВИСИМО ДРУГ ОТ ДРУГА. (1) Я сослался на SpectatePolicy.ShouldLogRefusal как на образец дросселя, а его док говорит дословно NOT A LOG ON CHANGE GATE and that is a deliberate departure — мой булев фронт был ровно отвергнутой формой, и под 5 процентов потерь заявка дрожит вокруг границы, давая две строки на колебание; дроссель переписан по форме прецедента, два поля на слот, бонд в секунду = TickRate тиков, та же частота что у собственного timing-update FishNet. (2) Два дока обещали ограниченность которой нет: при sanityTicks -4 оценка -1, каст byte даёт 255, и несущим оказывается кламп арены — замерено, обе фразы заменены правдой, клампа и правила в NetInvariants НЕ заведено, это вопрос владельцу. (3) Дополнение к рулингу 227 писало пять ПУБЛИЧНЫХ швов — их шесть и все internal; исполнитель красной фазы поймал это ещё тогда, я правку не внёс, и в круг ушла ложь. Остальные Important: B-1 две оси без свидетеля — все четыре фикстуры подавали capTicks 6 и pictureTicks 3, подстановка литералов переживала весь набор, а на чтении Arena.RewindPictureTicks держится довод §6h; заведены два теста, мутации M117 и M118 убивают ровно своего свидетеля и никого больше. A-3 объявлена вторая неосвидетельствованная ось (запись урезанного обратно во ввод). A-4 обещанный пункт гейта Ф4 не существовал: на него ссылались ЧЕТЫРЕ места кода, в плане ноль вхождений — заведена app-j511 discovered-from, план не переписан. B-3/B-5 три числа в доках были ложны: 2 это 33 процента а не 20, лишних тиков два а не четыре, нейтрализация на 3 а не на 6. ИСПОЛНИТЕЛЬ КРУГА ПРАВОК ПОЙМАЛ КООРДИНАТОРА НА ЛЕЧЕНИИ: мой наряд предписал написать что 2 это ближайшее целое к 20 процентам при капе 6, а 20 процентов от 6 это 1.2 и ближайшее целое 1 — он отказался писать предписанное и написал правду. Урок: на веру нельзя принимать не только находку но и лечение, включая предписанное координатором. ПРОГОНЫ: фильтр 6/6 EXIT=0; мутации M117/M118 каждая убила ровно своего свидетеля, md5 после ALLDONE совпал с базой; ПОЛНЫЙ ПРОГОН EXIT=2, 431 c, error CS 0, testcasecount 1776, зелёных 1773, красных ровно три — эталоны бит в бит те же что до круга, NetInvariantsTests остался 23; свипы дважды чисты, сцена не двигалась. Коммит 7ddddc1, пять файлов, +401/-107. Счёт мутаций эпика 223, рулингов 227. Эвиденс — task-29-review-evidence.md.


---

# ВЫГРУЗКА 2026-09-03 (сессия 86) — заметки, накопившиеся после выгрузки 2026-09-01

Колонка на момент выноса: 48201 байт (порог трека ~50 КБ). Ниже — строки колонки с третьей по последнюю дословно; первые две (указатель на этот файл и ПРИЧИНА) остаются в колонке.

ЧЕМ КОНЧАЛАСЬ: фаза Ф3 прошла ШЕСТЬ тасков из одиннадцати — Т24, Т25, Т26, Т27, Т28, Т29, у каждого круг ревью и круг правок. Т29 (санитарная проверка заявленной глубины) закрыт сессией 80 пятью коммитами: 437f0ea код, 583accc §6h спеки, 7ddddc1 круг правок, 5b96c14 решения владельца (правило #12 в NetInvariants и переименование шва), fe0af8d §6i спеки. Полный прогон на байтах коммита: 1779 тестов, 1776 зелёных, три красных — эталоны детерминизма, намеренно красные до Т34, все три бит в бит прежние. Мутаций эпика 223, рулингов 227, уроков 108-642.
ГЛАВНОЕ СОДЕРЖАНИЕ СЕССИИ 80: аудит нашёл верный факт (TimeManager.RoundTripTime на выделенном сервере тождественно ноль), но координатор сделал из него три неверных вывода подряд и попытался переделать апрувнутый план; владелец остановил. Эпик имеет записанный и четырежды применённый порядок для таких случаев — §6d/§6e/§6f/§6g: код исполняет план буквально, факт идёт в decision log спеки, правку делает владелец. Т29 закрыт этим порядком, факт записан §6h. Уроки 637-642.
РЕШЕНИЯ ВЛАДЕЛЬЦА 2026-09-01, записаны §6i спеки: §6f и §6h закрыты одним ответом — Arena.RewindCapTicks опускается 6 -> 5 (задача app-gtj6, едет ВМЕСТЕ с Т34, потому что фолд истории идёт по ёмкости кольца и понижение двигает эталоны); §6g закрыт вариантом а — неравномерность отмотанных часов остаётся и записана буквой.
ПРАВИЛО ДЛЯ СЛЕДУЮЩИХ СЕССИЙ: ноты снова принимаются — пиши сюда. Когда колонка снова подойдёт к 50 КБ, повтори приём: дописать дословно в docs/history/, закоммитить, заменить колонку указателем. Порядок обязателен — сперва история в git, потом правка трекера.
СЕССИЯ 81, АУДИТ Т30 (аудит лично, приём 543, с диффа эпика 3cb379d..fe0af8d — 147 файлов, пересечение с домами Т30 десять из двенадцати). Девять находок, три несущие. A-Т30-1 (Critical): бросающих таблиц ЧЕТЫРЕ, а не три — четвёртая это расширяющий switch SnapshotAssembler.BeginTick, чей default бросает ArgumentException В БОЕВОМ ТИКЕ СЕРВЕРА, а не в кодеке; его док сам называет себя девятым домом каталога событий; существующий свидетель UnmappedEventKind_ThrowsInsteadOfVanishing кормится значением 99 и забытый кейс не поймает. A-Т30-2 (Critical для красной фазы): набор красных плана занижен — свип по признаку (урок 559) дал пять обходов перечисления, из них четыре в SnapshotCodecTests и ОДИН в EventDeliveryTests (ChannelFor_HandlesEveryKind), которого фильтр Step 2 плана не гоняет вовсе; плюс SnapshotEventKind_ValuesArePinned останется красным и ПОСЛЕ green, если не дописать пин значения 19. A-Т30-8 (Critical по цене): Т30 НЕ проводной таск по верному критерию RULING 202 — замер списка GWrite___/GRead___ в собранной Ring.Networking.dll даёт 33 записи и семнадцать типов, среди них НЕТ ни SnapshotEventKind, ни SnapshotEventPayload, ни SimEventKind, а SnapshotBroadcast это Tick плюс MatchEpoch плюс ArraySegment байт, то есть каталог событий едет рукописными байтами внутри Payload и форма броадкаста от нового вида не меняется; сам план в Step 7 требует только пустой гейт-кодоген, без слова целиком и без артефакта — требование целиком плюс артефакт есть утверждение handoff-а. Прочие: VisibleSubjectId точкой касания НЕ является (рикошет обязан идти в канал None как все пять Projectile-видов, а VisibleSubjectId достижим только из ветки Visible); SimEventRouter в Files, но работы в нём нет — это чистый фан-аут без switch по виду; шов EndsProjectileForTest обязан стать НЕСУЩИМ, иначе тест плана тавтология, потому что правила как функции сегодня не существует; искру писать не надо — PersistentPropsDirector.HandleRicocheted уже делает ровно то и уже несёт гвард нулевой нормали. ПРЕДСКАЗАНИЕ ЭТАЛОНОВ (вопрос 627 задан первым, отвечен замером): ни один не сдвинется — StateHash не фолдит события вовсе, DroppedEvents тоже не в фолде, симуляция свой буфер событий внутри тика не читает (свип EventCount по Scripts/Simulation вне SimulationWorld даёт ноль). Сдвиг любого из трёх = находка и стоп. Полный аудит — task-30-audit.md.
СЕССИЯ 81, Т30 ЗАКРЫТ ЦЕЛИКОМ (ProjectileRicocheted на проводе). Четыре коммита: ca584cb код (12 файлов, +814/-22), cafc2f0 §6j спеки, c421aae круг правок, 1a54a9a §6j второе расхождение. Полный прогон на байтах последнего коммита: 1785 тестов, 1782 зелёных, три красных — только эталоны детерминизма, намеренно красные до Т34, все три бит в бит прежние (6179821658852201134 / 12948336956096453079 / 12266730622305861208). Перепин НЕ потрачен. testcasecount 1779 -> 1785 (шесть новых тестов). error CS 0 во всех прогонах.
ЧТО ТЕПЕРЬ УМЕЕТ ИГРА: отскок снаряда от статической геометрии перестал быть немым. Сервер шлёт запись с точкой контакта и нормалью, клиент рисует искру и играет клип отскока. Несущее свойство: рикошет это СЕРЕДИНА жизни снаряда, поэтому он НЕ закрывает подписку соединения на траекторию — конец полёта после отскока по-прежнему доходит до того же зрителя, и это проверено в наблюдаемой форме, а не вопросом к предикату.
МУТАЦИИ: 27 штук (M122-M148). 19 убиты предсказанными жертвами с первого захода. ЧЕТЫРЕ пережили весь сьют и были закрыты свидетелями внутри таска: оба дома клиентского декодера (ProjectileRicocheted оказался единственным видом каталога без именного теста в ClientEventDecoderTests) и правильность вектора нормали в эмите. Две не гнались по замеру — презентация в EditMode свидетеля не имеет, приёмка В3. Ещё две (M147/M148) пришли из круга ревью и убиты новыми ассертами.
ГЛАВНАЯ НАХОДКА ЦИКЛА: док зелёного теста утверждал, что подмена нормали на скорость умирает на ассерте знака скалярного произведения. Прогон опроверг: TryRicochet отражает скорость ВНУТРИ себя, эмит стоит после, поэтому proj.Vel уже смотрит внутрь арены и при перпендикулярном ударе коллинеарен нормали. Различает только длина: нормаль обода единична по построению, отражённая скорость несёт скорость*Retention. Ни одна фикстура дерева не варьирует угол контакта (урок 640).
КРУГ РЕВЬЮ (два ревьюера, разные углы): Critical 0, Important 6, Minor 5, ложных 0, боевого дефекта нет. ТРИ находки сделаны независимо друг от друга: заголовочная точка записи (та, что рисует искру) не имела свидетеля ни для рикошета, ни для конца полёта с Т28; премисса таска про поворачивающий трассер ложна для сетевого клиента; ширина payload — ничья с PlayerDamaged, а не превосходство.
ГЕЙТЫ: гейт-кодоген гнался ЦЕЛИКОМ по решению владельца 1b, включая пересборку linux-server (EXIT=0, 45 c, error CS 0, codegen error 0; в артефакте Comparer___/GWrite___Unity/GRead___Unity ноль). Артефакт несёт строку ProjectileRicocheted — Т30 доказуемо в собранном сервере. Сборка от 1280593 затёрта, предупреждение выдано заранее. Замер RULING 235 воспроизвёлся: 33 кодогенные записи, все Ring/System, ни одной Unity — то есть по верному критерию таск НЕ проводной, гнали целиком по решению владельца. Свипы дважды, все чисты. ProtocolVersion 4 не двигалась. Сцена 269 / 0241c714945a15bf1c68060f075209a4.
⚠ ЖДЁТ ВЛАДЕЛЬЦА, всё собрано в §6j спеки: (1) payload 7 Б против шаблона самой спеки в 3 Б — четыре байта не читает в бою никто, рекомендация сузить отдельным таском пока вид не отгружен; (2) контактная высота рикошета не доезжает до клиента и искра встаёт на пол, тогда как соседний HandleBlocked ставит свою на высоте контакта — рекомендация решать вместе с первым, байт высоты ложится в сужённый payload свободно; (3) ОЖИДАНИЕ ПЛЕЙТЕСТА В3: до Т32 по сети пуля идёт сквозь стену, у которой уже есть искра, потому что сетевой трассер это замкнутая форма и модель полёта на клиенте никто не крутит (StepTo в дереве ноль вхождений).
ПРОБИТИЕ вынесено отдельной задачей app-tbvg (поднята до P1, RULING 236): механика выключена числами — порог массы 43.33 кг у пули сборщика против самого лёгкого тела игры 70 кг, то есть не срабатывает ни разу; плюс payload не влезает в потолок 8 Б и крита ещё нет.
Эвиденс: task-30-audit.md, task-30-rulings.md (228-242), task-30-red-evidence.md, task-30-green-evidence.md, task-88jb-30-mutations-predicted.md, task-30-mutation-evidence.md, task-30-review-evidence.md, task-30-report-red.md (все фазы). Счёт эпика: мутаций 250, рулингов 242.
СЕССИЯ 82 (2026-09-02), СТАРТ Т31 (ProjectileEnded 5 -> 8 Б). Сверка сошлась целиком (main 4a20699, ветка 1a54a9a, деревья чисты, 1785/1782/3). Решения владельца: §6j расхождения 1 и 2 — вариант б, сужение payload рикошета до 4 Б отдельной задачей app-5o2q после Т31, до гейта Ф3; MobVisual.cs и ViewRegistry.cs входят в Files Т31 решением владельца (прецедент AudioDirector Т30 — у MobVisual нет входа для события кроме SetHitDir, зовёт его только ViewRegistry); app-0ogx закрыта — исполнена владельцем в main коммитом d30dc33 от 25.08; гейт-кодоген Т31 — дешёвая половина, пересборка linux-server только по слову владельца; ожидание В3 (до Т32 по сети пуля идёт сквозь стену с искрой) — к сведению. Условие владельца на все решения — пять правил AGENT.md §4. Соседняя сессия the-ring-f9 — на стороне владельца. Наблюдения до аудита: высота для HitMob теряется на ОБОИХ концах (ассемблер пишет 0f, декодер не читает); тест ProjectileEnded_HitMob_..._LeavesTheVictimUnclaimed и класс-док декодера пинят противоположное Т31 — переписываются.
Т31, решение владельца по развилке Step 4 (аудит A-Т31-3): вариант б — крен моба по сети синтезирует NetworkSimBackend (новый класс Networking/Client/MobTiltIntegrator, импульс той же парой Impact.VelocityDelta -> AngularImpulse, шаг SpringStep по TickDt, подстановка в опубликованную пару после ApplyOwnPlayer), MobVisual/ViewRegistry кодом не трогаются. Цель словами владельца: вспышка там, где попали, и моб кренится; импакт должен ощущаться механически и физически. Рулинги 243-258 в task-31-rulings.md, аудит в task-31-audit.md, §6k спеки дополнен.
Т31 RED закрыта (координатор лично, пять фильтров): 15 красных ассертом, все предсказаны именем/ассертом/текстом, исключений 0, error CS 0, testcasecount 1785 -> 1796, отрицательная половина 176 зелёных. Эвиденс task-31-red-evidence.md. RULING 261 — находки исполнителя (три дока Simulation в список правок, второй способ промаха импульса назван). Дальше — зелёный наряд.
Т31, ПАУЗА перед компактом (сессия 82). GREEN написана (15 файлов), пять фильтров зелены (106/32/34/14/5, testcasecount 1796), полный прогон был в полёте; свипы по новым строкам нули; кодоген дешёвой половиной — 33/0 Unity. Не сделано: вердикт полного прогона, цикл мутаций (наряд task-31-brief-mutations.md готов), коммит, круг ревью, правки, handoff. Точка возобновления: task-31-resume-point.md.
Т31 GREEN закрыта эвиденсом (task-31-green-evidence.md): пять фильтров 106/32/34/14/5 зелены, полный 1796/1793/3 с тремя эталонами на прежних But was (6179821658852201134 / 12948336956096453079 / 12266730622305861208), error CS 0, свипы 0, кодоген 33/0 (RULING 202 нет), md5 DeterminismTests прежний. Дерево: 16 M + 4 новых поверх ddda129, не закоммичено. Дальше: цикл мутаций M150+, гейты, коммит, круг ревью.
Т31 мутации фаза A: 31 прогон, 29 убиты предсказанными жертвами (тексты и числа сошлись), 2 выжили как предсказано — M164 (HitPlayer EntityId=VictimId, фикстура даёт ноль) и M176 (своп-ремув с i++, одно тело). error CS 0, откат md5 31/31. M180 (MobConfigFor) имеет жертву через мир — RULING 263, прямой тест не пишется. RULING 264: долг свидетелей закрывается внутри таска (task-31-brief-witness.md). Фаза B (полный прогон с обеими) в полёте.
Т31 закоммичен d91b32e (20 файлов, +1766/-127): провод 8 байт, ассемблер/декодер, TryGetOwner, MobTiltIntegrator + NetworkSimBackend, SimConfig.MobConfigFor, доки. Свидетели M164/M176 закрыты (RULING 264), перегон — обе умирают; полный прогон 1797/1794/3, эталоны прежние; кодоген 33/0; свипы 0. Дальше: круг ревью двумя ревьюерами, правки, полный прогон на байтах правок.
Т31 круг ревью d91b32e: 17 находок (A 6, B 11); A-1 Critical опровергнута (_lastRenderTick присваивается каждый Advance), B-1 опровергнута (мутант гварда неотличим); боевой код не меняется. Круг правок: два свидетеля (Blocked-арм, гварды интегратора) + 11 доков; B-6 (GameFeelDirector.cs, чужая зона) — вопрос владельцу. RULING 265-267.
Т31 ИТОГ: d91b32e (код) + bbfb280 (круг правок по ревью: свидетели арма Blocked и гвардов интегратора, 11 доков). Полный прогон 1799/1796/3, эталоны прежние; мутации M150-M188: 33 убиты предсказанными жертвами, M181-M184 без EditMode-свидетеля (плейтест Ф4), долг M164/M176 закрыт внутри таска. Рулинги 243-268. Владельцу: B-6 док GameFeelDirector (чужая зона), out owner у TryGetOwner, (урок 1xx) прошлого эпика. Дальше по слову владельца: Т32.
Конец сессии 82, решения владельца: скоуп следующей — app-5o2q, затем Т32; app-fjch (док GameFeelDirector, санкция на чужую зону) и app-3c4i (chore кириллицы SnapshotCodecTests) — мини-коммиты до первого наряда; out owner у TryGetOwner остаётся. Handoff обновлён.
Т32 (сессия 84), аудит лично приёмом 543 с диффа эпика 3cb379d..510f6b3 — эвиденс task-32-audit.md. Пять адресных цитат плана из семи МЕРТВЫ (WriteInto :194 -> :243, Prune :218 -> :267, конструктор бэкенда :1494 -> :1653, обе записи пары :1230 -> :1342, бутстрап :951 -> :1232); счёт конструкторов в плане занижен — десять в TracerProjectilesTests, а не восемь, всего четырнадцать точек. Тринадцать находок, три несущие. A-Т32-1 (Critical по цене): план не называет аргументы обеих записей рендер-пары, а сегодня это renderTick/renderTick+1 — оставить как есть значит не сдвинуть на экране ни пикселя; прочтение подтверждено ценой, которую называет сама спека (снаряд опережает чужие тела на глубину). A-Т32-2 (Critical): предсказанного тика на рендер-кадре нет ни в одном поле — MeasureRewindTicks приватна, зовётся раз за тик предсказания и результат уезжает в запись ввода; форма решается рулингом, свидетеля у проводки нет и быть не может (судья — лаг-гейт Ф4). A-Т32-3: премисса плана про разрыв 5-7 м ЛОЖНА — число в шапке гостов это телепорт, которого Р67 не допускает, а гост вообще не рисуется (позиций наружу не отдаёт ни одной); исполнить букву значит записать в док новую ложь. Прочие: AllocationTests не знает трассера вовсе (Step 7b пуст, спека §4.3 требует свидетеля); тест плана про своп кэша не зовёт Prune и свидетелем не является; класс-док трассера станет ложью в ЧЕТЫРЁХ местах, план называет два; app-56kx жива — замерено телом AddProjectileSpawned: позиция едет из СОБЫТИЯ (дуло), скорость/высота из ЗАХВАТА конца тика, XY отстаёт на k_ввод+1 шагов = 1.75-7.0 м при шиппинговых 52.5 м/с.
Т32, рулинги 285-299 записаны ДО первого наряда — task-32-rulings.md. Решение владельца 2026-09-02: шов app-56kx закрывается ЦЕЛИКОМ (обе половины отставания), потолок payload поднимается 8 -> 9. Форма (Р291): WeaponSystem уже считает RewindSplit.InputTicks для CatchUp — то же число плюс единица (обычный шаг тика рождения) уезжает параметром в SpawnProjectile и в SimEvent тем же вызовом Emit, что и сегодня (перенос Emit после CatchUp ЗАПРЕЩЁН — пустил бы события догона вперёд события рождения, а на порядке стоит правило подписки ассемблера); ассемблер кладёт число девятым байтом; клиент сеет трассер точкой decoded.Pos + dir*horizSpeed*dt*steps, высоту НЕ трогает — она уже из захвата и сходится точно. Вариант с переносом точки заголовка СНЯТ ЗАМЕРОМ: на ней висит косметика — вспышка мобьего выстрела бьёт из неё, гильзы сборщика по ней записываются. Р292: цена потолка ПЕРЕМЕРЕНА и НИЖЕ handoff-овской — он обещает двенадцать строк страйдов, замер даёт девятнадцать употреблений MaxPayloadBytes в ассемблере и НИ ОДНО не требует правки (все масштабируются константой); правятся константа, её док, пин AreEqual(8,...), пин ширины вида, арм писателя и читателя. ProtocolVersion НЕ двигается — прецедент Т8 (PlayerDamaged 4 -> 7 без бампа). Тот же потолок — одно из трёх условий app-tbvg, после Т32 оно исполнено. Прочее: Р285 пара трассера и Prune едут на предсказанном тике (иначе таск не виден на экране); Р286 глубина защёлкивается на тике предсказания, свидетеля у проводки нет — судья лаг-гейт Ф4; Р287 WriteInto — замкнутая форма ОТ КЭША (иначе падают все четырнадцать существующих тестов); Р288 кэш полем внутри Track, мутант свопа непредставим, тест плана объявляется сторожем; Р289 трассер встаёт у геометрии и ждёт события, высотный гейт барьера выносится публичным помощником в ProjectileFlight (один дом, два вызывающих — форма RULING 92); Р296 фикстура теста 48 плана слепа к шву по построению — свидетель шва обязан гнать выстрел ЧЕРЕЗ WeaponSystem; Р297 точек конструктора четырнадцать, а не одиннадцать.
Т32 разбит координатором на ДВА наряда: 32-А — провод и симуляция (шов рождения, девятый байт), 32-Б — сам трассер (кэш, StepTo, рикошет, бюджет догона). Порядок жёсткий: тесты трассера опираются на байт, который заводит 32-А. Наряд красной фазы 32-А отправлен (task-32a-brief-red.md): SimEvent получает BirthSteps, Emit и SpawnProjectile — хвостовой параметр с умолчанием, WeaponSystem кормит его тем же локалом InputTicks, что уже считает для CatchUp (перенос Emit после CatchUp ЗАПРЕЩЁН — порядок эмитов несёт правило подписки), MobAiSystem пишет единицу явно; MaxPayloadBytes 8 -> 9, ProjectileSpawned 8 -> 9, писатель ЯВНО зануляет байт 8 (Reserve буфер НЕ зануляет — замерено, пул переиспользуется), читатель байт не читает. Предсказание красной фазы координатора: testcasecount 1800 -> 1802 (два новых [Test]), четыре красных ассертом, исключений ноль, три эталона с ПРЕЖНИМИ But was — довод строится ЗАНОВО, потому что Simulation тронута (поле события и параметр спавна), и вопрос ставится как -входит ли хоть одно в StateHash-. Клиент в этом наряде НЕ трогается: RouteToTracers прочтёт p.BirthSteps напрямую, копия в SimEvent завела бы второго носителя одного числа.
Т32-А, КРАСНАЯ ФАЗА ЗАКРЫТА — эвиденс task-32a-red-evidence.md, пять прогонов координатора лично. SnapshotCodecTests EXIT=2 25c 107/106/1, EventDeliveryTests EXIT=2 20c 35/34/1, RewindTests EXIT=2 20c 44/43/1, ClientEventDecoderTests EXIT=0 32/32, DeterminismTests EXIT=2 161c 23/20/3. testcasecount 1802 во всех пяти (предсказано 1802), error CS 0 везде, Csc 8 на первом. Три красных, все ассертом, все названы заранее по имени, месту и тексту: byte 8 Expected 3 But was 0; мировой свидетель Expected 4 But was 0; свидетель шва Expected 2 But was 0 (глубина 4). Исключений ноль. ОТРИЦАТЕЛЬНАЯ ПОЛОВИНА ДЕРЖИТСЯ: декодер 32/32, три эталона БИТ В БИТ прежние (6179821658852201134 / 12948336956096453079 / 12266730622305861208) — довод про отсутствие канала до StateHash подтверждён замером, перепин Т34 не потрачен. Я предсказал пять красных сверх эталонов, факт три, оба расхождения В МИНУС и объясняются формой лечения: отдельного round-trip у вида в дереве НЕТ (ассерты живут внутри теста раскладки, NUnit останавливает на первом), а тест худшего кадра вылечен как ПИН — исполнитель сделал выражения производными от константы, а не подставил число. Пять находок исполнителя проверены лично открытием файла. F-1 ПОПРАВЛЯЕТ МОЙ АУДИТ: гильзы точку события не читают вовсе (RecordCasing кладёт только PlayerIndex, брасс летит из EjectSocket куклы), зато четвёртый потребитель, которого я искал, — сам SnapshotAssembler: отрезок релевантности Р32, позиция заголовка и слышимость ShotHeard, то есть сдвиг точки менял бы КОМУ раунд отправляется. Заведён RULING 300: байт получает домен на чтении верхней границей cap+1 (не точной — точная завела бы второй дом правила деления; прецедент формы RULING 165).
ПАУЗА ПЕРЕД КОМПАКТОМ (сессия 84). Точка возобновления — task-32-resume-point.md, читать первой. Состояние снято машиной ПОСЛЕ подтверждённой остановки: ветка 510f6b3 (коммитов Т32 ещё нет), main 9044e59, дерево ветки 12 строк статуса все M, новых файлов ноль, 521 вставка / 66 удалений — это красная фаза Т32-А и её НЕ откатывать; в main одна строка дрейфа .beads. Stash пуст, маркеров мутаций ноль, Unity не запущен, lockfile нет, md5 DeterminismTests не двигался, сцена 269/0241c714945a15bf1c68060f075209a4. Агентов в работе нет, соседних сессий трека нет. НЕ СДЕЛАНО по порядку: зелёная фаза Т32-А (снять три заглушки — SpawnProjectile не передаёт число в Emit, писатель зануляет байт 8, читатель байт не читает — и закрыть RULING 300 доменом байта с ДВУМЯ ассертами: отказ за границей и приём НА границе); прогон зелёной четырьмя фильтрами плюс полный; наряд красной фазы Т32-Б (трассер: кэш, StepTo, рикошет, бюджет догона, конструктор с SimConfig, проводка бэкенда) — не написан, содержание задано рулингами 285-299; дальше цикл мутаций с M211, гейты, коммит, круг ревью, круг правок, ледгер, chore-коммит дрейфа, push. Гейт Ф3, веха В3 и PR — не этой сессии без слова владельца.
Т32-А (шов рождения на проводе) закрыт коммитом 334ccc0.

Проведено: SimEvent.BirthSteps + хвостовые параметры Emit/SpawnProjectile;
MaxPayloadBytes 8->9, девятый байт ProjectileSpawned; домен на чтении
верхней границей cap+1 (RULING 300), сторож ширины байта на записи
(RULING 301), фикстура EvtCfg RewindCapTicks=4 (RULING 302).

Эвиденс: пять прогонов, testcasecount 1800->1803, полный прогон 1800/3,
три эталона бит в бит прежние (перепин Т34 НЕ потрачен). Гейт-кодогена
Р202 перемерен заново: НЕТ. Цикл мутаций M211-M217, восьмь мутантов,
выживших ноль; пара M214/M215 доказывает, что свидетель меряет ГРАНИЦУ.

Осталось по Т32: половина Б (сам трассер) - наряд task-32b-brief-red.md,
рулинги 285-299 и 303-306. Не начата.
Т32 ЗАКРЫТА ЦЕЛИКОМ. Коммиты: 334ccc0 (А, провод), d5ddc3c (Б, трассер),
bbf0df9 (круг правок по ревью), 1a24ca1 (decision log спеки).

Эвиденс: testcasecount 1800 -> 1822, полный прогон 1819/3; три эталона бит в бит прежние
через ВСЕ прогоны сессии, перепин Т34 НЕ потрачен. Гейт-кодогена Р202 перемерен: НЕТ.
R-APPLY/R-IDEM пройдены: NetConfig.asset получил ровно одну строку.

Мутации M211-M229. Т32-А: восемь из восьми. Т32-Б: девять мутантов, ПЯТЬ ВЫЖИЛО, три из
них я предсказал смертниками — все пять закрыты свидетелями, каждый проверен повторным
прогоном. Круг ревью нашёл ШЕСТОГО выжившего (вызов высотного гейта из трассера не
проверялся ничем) — подтверждён прогоном, закрыт свидетелем.

Урок 664: мутировать несущее, а не удобное. За одну задачу повторён четырежды.

Ревью: 2 CRITICAL, 9 IMPORTANT, 11 MINOR, ложных ноль. Три находки отданы владельцу
задачами app-pj5t, app-2icd, app-9c4m (+ app-6uw1), все через discovered-from.
app-56kx закрыт с эвиденсом.

⛔ ГЕЙТ ФАЗЫ Ф3, ВЕХА В3 И PR — НЕ СДЕЛАНЫ И НЕ НАЧАТЫ: требуют отдельного слова
владельца. Остаток фазы по плану — Т33 (амендменты ADR) и Т34 (перепин эталонов).
Т33 (сессия 85) СТАРТ. Документарный таск: амендменты ADR одной пачкой, свипом по пяти документам (план 6167-6200). Кода нет, тестов нет, Unity не нужна. Сверка состояния машиной сошлась целиком: main 3020229, ветка feature/app-88jb-weapon-netcode 1a24ca1, обе на origin живым ls-remote; оба дерева 0 строк, стешей нет; md5 DeterminismTests.cs = dbd00b04d6ef17d470fd80e5aab38fb7; процессов Unity 0, UnityLockfile отсутствует; git diff main..feature -- docs/adr/ ПУСТ (дрейфа ADR через симлинк нет). Свип по букве плана (hitstop|хитстоп|снаряд|рикошет|отмотк|lag comp) дал 23 строки: ADR-001 11, ADR-002 10, ADR-003 1, SETUP-ПО 1, ASSETS-001 НОЛЬ. Узкий паттерн не покрывает ASSETS-001 и CR 3 вовсе, поэтому гнался второй, широкий свип по лексике эпика (масса/импульс/крен/части тела/кокон/принтер/обвес/предсказание/интерполяция/высоты): ADR-001 24, ADR-002 26, ADR-003 23, SETUP-ПО 0, ASSETS-001 9. Инвентарь пишется файлом в $SDD/task-88jb-33-adr-sweep.md (урок 471).
Т33 ЗАКРЫТА КОММИТОМ 8bd46f1 (ветка feature/app-88jb-weapon-netcode). Свип дал ДВЕНАДЦАТЬ записей против девяти названных планом — внесены все (урок 471). ADR-001 §14: A10 лор Р370, A11 масса+рикошет+оговорка 300 м/с, A12 части тела, A13 жёсткое разведение, A14 hitstop из чеклиста. ADR-002 §10: A24 lag compensation явно (две половины, кап 6 тиков, глубина в вводе, понижение до 5 едет с app-gtj6), A25 УТОЧНЕНИЕ CR 3 по Р419, A26 §5 та же модель полёта на предсказанном тике, A27 вторая прописка hitstop в §8. ADR-003 §11: A6 лор оболочки/оружия/кокона, A7 словарь §9 семью строками с Ricochet (НЕ Bounce). SETUP-ПО.md: секции Amendments не было — заведена, A1 про Feel (пакет не покупался, замерено по ThirdParty и manifest.json). ASSETS-001: пункты 9-10 — высоты из измерения крон, элита k=1.0216. ШАГ 3 ДОКАЗАН МАШИНОЙ: дифф 346 добавлений и НОЛЬ удалений; исходные строки каждого файла побитово равны HEAD (md5 совпал у всех пяти, у ASSETS-001 — по двум частям вокруг вставки); все добавления лежат внутри секций поправок. ⛔ ТРИ УТВЕРЖДЕНИЯ ИСТОЧНИКОВ КОД НЕ ПОДТВЕРДИЛ, в ADR ушла сверенная версия: (1) спека §3.5 обещает, что дэш-кокон расталкивает мобов и сборщик траектории не меняет — поля DashPushSpeed нет вовсе, а BodySeparation вычитает принятый толчок из DashSpeedCur проекцией (решение Р443, находка Н-42); (2) «только видимые тела» — правило КЛИЕНТА, SeparationSystem.SnapshotBodies фильтра видимости не имеет (уже записано спекой §6c, находка I-3 ревью Т22, сегодня no-op по арифметике 0.95 против 2.15 м); (3) handoff §6.2 п.13 приписывает долг Bounce задаче app-215e — та про modelled; замер дал ШЕСТЬ Bounce в ЧЕТЫРЁХ файлах, и одно вхождение завёл САМ ЭПИК (TracerFlightTests.cs:762, файл создан Т32), ревью Т32 не поймало ⇒ заведена app-3ptw. ⚠ ЛОВУШКА СИМЛИНКА: $RING_ROOT/ADR смотрит в MAIN-дерево, амендменты лежат в ВЕТКЕ — владелец их через симлинк не увидит до мержа. Инвентарь и все замеры — $SDD/task-88jb-33-adr-sweep.md (203 строки). Счётчик тестов таск не двигал: кода нет, Unity не запускалась.
Т33 КРУГ РЕВЬЮ И КРУГ ПРАВОК — коммит 3c20d87. Один ревьюер, ВОСЕМЬ находок, ЛОЖНЫХ НОЛЬ, каждая перепроверена координатором лично командой (правило 626). Эвиденс — $SDD/task-33-review-evidence.md. C-1 (CRITICAL): ADR-001 A12 и ASSETS-001 п.9 писали «вся колонка масштабирована на измеренный k», но у СБОРЩИКА она не масштабирована намеренно — спека §3.3 даёт ему k=1, высоты остались 0.55/1.35/1.75, а Т12 померил 1.0481 (крона 1.8342 против колонки 1.75) ⇒ верхние ~8 см его модели ВНЕ объёма попадания; док HeroConfig.cs:140-153 называет это явно и с числом. Внесено в оба документа с последствием и пометкой для PvP Этапа 4. C-2 (CRITICAL): A13 приводил арифметику паритета (перекрытие < 0.95 м против 2.15 м между телами по разные стороны блокера), тогда как ЕДИНСТВЕННЫЙ боевой вызов PlayerNetworkController.cs:569 передаёт PlayerPrediction.Step ПУСТОЙ набор тел — в сети клиент не разводится вообще, каждый контакт с мобом даёт рывок поправки; это открытая P1 app-njmi, и она стоит в таблице §1 handoff-а, который я читал целиком. Добавлен абзац: правило и его арифметика — про устройство, а не про сегодняшний билд. I-1: A25 описывал применение импульса настоящим временем, а проводки нет — боевой вызов передаёт ImpactPulse.None, у ImpactPulseLog ноль продакшен-вызывающих, решением Р434 проводка перенесена в Ф3 задачей app-7du2 (сперва анализ: кольцо адресуется тиком МИРА, спрашивается тиком FISHNET, моста нет). ЧЕТЫРЕ MINOR закрыты: пять полей плюс enum-тип ПРЕЗЕНТАЦИОННОГО SO вместо «шести полей балансного» (GameFeelConfig не входит в SimConfig вовсе); правило валидации 14 сторожит верх последней части Директора 4.80, запас 0.10 м, а не крону 4.7903; рюкзак живёт в ADR-003 §7.2, а не §7.4; «кокон не усиливается» получило область «в заходе», иначе читатель снял бы межзаходную прокачку кокона из A1. Плюс поправлен собственный инвентарь свипа: записей не двенадцать в четырёх документах, а ЧЕТЫРНАДЦАТЬ В ПЯТИ (строка SETUP-ПО в таблицу не попала, ASSETS-001 стояла одной строкой при двух пунктах). ⛔ ГЛАВНЫЙ УРОК КРУГА: обе CRITICAL — один класс ошибки. Числа я проверил по коду (четырнадцать проверок, все сошлись), а КВАНТОРЫ ОХВАТА («вся колонка», «клиент разводится») оставил на веру к спеке. Число проверяется грепом за секунду; квантор требует пройти по всем потребителям. ⇒ у каждого «весь/всегда/сегодня» в амендменте спрашивать, чем он замерен. ШАГ 3 ПЕРЕДОКАЗАН ПОСЛЕ ПРАВОК: суммарно против 1a24ca1 дифф даёт 386 добавлений и НОЛЬ удалений, ни одной строки с дефисом; побитовая сверка исходных строк с 1a24ca1 сошлась у всех пяти файлов (у ASSETS-001 — вокруг вставки 47-102, хунк @@ -46,0 +47,56 @@).
СЕССИЯ 85 ЗАКРЫТА. Т33 закрыта двумя коммитами (8bd46f1 амендменты, 3c20d87 круг правок), ледгер дописан, handoff обновлён ПОСЕКЦИОННО по HANDOFF_PROTOCOL.md под сессию 86 со скоупом Т34. Проверки протокола: все 12 секций эталона на месте, §6.0 «что в дереве и почему» заполнена свежим git status (оба дерева 0 строк), md5 защищённой секции параллельного трека app-zuo = 3b0a157f5dde8dd48d5d63e3acabfae0 СОВПАЛ до и после правок, заголовок той секции встречается РОВНО ОДИН РАЗ, секретов в файле нет. Прежняя версия handoff-а сохранена как $SDD/handoff_app_server.before-85.md для диффа. ЧТО ВАЖНОГО ПЕРЕНЕСЕНО В HANDOFF ДЛЯ Т34: (1) план Т34 местами УСТАРЕЛ и это замерено — прежний md5 c24883fd... мёртв (живой dbd00b04d6ef17d470fd80e5aab38fb7), из адресов :435/:1321/:1497/:1513 живой только :435, файл вырос до 1684 строк, живые адреса :435 извлечение / :1374 соло / :1550 мульти / :1566 тест покрытия; (2) счётчик 1822/1819/3 — свойство коммита 1a24ca1, а не замер: Т33 Unity не запускала ни разу, поэтому первый шаг Т34 (R-TEST) его перемеряет, и четвёртый красный = СТОП; (3) порядок шагов 2->3 НЕОБРАТИМ — сперва расширить покрытие семью механиками, потом снимать числа; (4) app-gtj6 (RewindCapTicks 6->5) едет ТЕМ ЖЕ заходом, правка капа бутстрапом ДО снятия чисел, иначе эталоны пинятся дважды при одной санкции; (5) гейт Ф3 требует ШЕСТИ целей сборки и затрёт все текущие builds/ — предупредить владельца ЗАРАНЕЕ; (6) колонка app-wpei = 45 459 Б при пороге ~50 КБ, возможно архивировать ДО первой ноты Т34. Уроки 668-670 записаны, правило §5 п.16 поднято до «уроки 609-670». Исправлена ложная запись §6.2 п.13 прежнего handoff-а (долг Bounce приписывался app-215e; на деле app-3ptw, шесть вхождений в четырёх файлах).