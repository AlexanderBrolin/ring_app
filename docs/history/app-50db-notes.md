# История задачи `app-50db` — архив колонки `notes`

**Задача:** Ф2: геометрия и полёт — части тела, рикошет, пробитие, разведение тел (веха В2)

**Тип:** task · **Приоритет:** P1 · **Статус:** in_progress

⚠ **Это архив, а не рабочий документ.** Колонка `notes` в bd ограничена 65 535
байтами и была заполнена под потолок — `bd note` переставала проходить вовсе
(`Error 1105`), то есть итог очередного таска физически некуда было положить.
Решение владельца 2026-08-29: историю вынести сюда, а в трекере оставить
короткий указатель. Ниже — колонка `notes` **дословно**, на момент выноса.

**Выгружено:** 2026-08-29 · **объём:** 64489 байт

---

Вход: план v3, таски Т12-Т23, веха В2. Из круга 3: фикстуры с ходьбой/подкатом/боевым выстрелом берут TestConfigs.OpenField(), а не Open() (Open спавнит сборщика в 159 м); фикстура рикошета Т19 строится от Quiet(), а не Default() (у Default живые волны); Т19 вводит ветку 'контакт с dot>=0 не разрешается вовсе' — десятое отклонение от спеки; M21 нацелена на ...ObservedThroughTheWorld; M23a и M41-слоты правил 9/10 получили свои шаги; M25 переформулирована (правило 'только видимые' под LoS-видимостью no-op). ⚠ ЧИСЛА ЧАСТЕЙ ДИРЕКТОРА В Т13: владелец подтвердил плановую позицию — проход подката под Директором НЕ открывается, ноги остаются [0, 1.51).
Т16 закрыт (9812e9f): числа частей и MaxAimHeight в .asset, app-f6yp закрыт. Фаза прошла ПЯТЬ тасков из двенадцати (Т12-Т16). Rulings 83-85. ⚠ Т17 наследует три долга: перестройку AimProxy_* из массива частей (ОБЕ точки бутстрапа), снятие SO-колонки (Ruling 76 - свип штатно даёт 43 строки в 6 файлах, ожидание 'ноль' появится только после Т17) и док-ложь StageOneSceneBootstrap.cs:3723-3726 (M-1 ревью Т15). Плюс новый долг Т16: ApplyDirectorDefaults док :3029-3035 утверждает 'the column is what decides a shot' - устарел на Т14/Т15.
Т17 закрыт (1caa371). Фаза прошла ШЕСТЬ тасков из двенадцати (Т12-Т17). Rulings 87-89. Три долга сверх плана закрыты все: Ruling 76 (SO-колонка снята), M-1 (док EnsureAimProxyChildren врал с Т15), F3 (док ApplyDirectorDefaults 'the column is what decides a shot' врал с Т14/Т15 — абзац сам называл Т17 условием своего исчезновения, ушёл целиком). Ruling 89: две строки комментариев в SnapshotCodecTests/InputCodecTests взяты в скоуп, потому что ложь создаёт ЭТОТ таск (прецедент — аудит Т16 §5.3, тот же файл, тот же класс лжи); благодаря этому свип колонки после = 0, а не 2. ⚠ ЦЕНА, НАЗВАННАЯ ЧИСЛОМ: 30 мёртвых ключей колонки в пяти .asset плюс AimProxyHeadRadiusFrac: 0.8 в GameFeelConfig.asset остаются — прецедент Ruling 44/52, .asset руками не правятся. ⚠ Первосоздательный путь EnsureAimProxyChildren не исполнялся ни разу: префабы существуют, работу делает SelfHealAimProxyOnPrefab. Гейт фазы Ф2 требует обе точки поимённо — вторая проверена R-APPLY и двумя R-IDEM, первая держится на том, что это тот же однострочный вызов той же функции. Дальше Т18.
Т18 закрыт (9b09f59). Фаза Ф2 прошла СЕМЬ тасков из двенадцати (Т12-Т18). Ruling 91. Дальше Т19 (рикошет) — он создаёт ProjectileFlightTests.cs и правит док Trajectory.cs:39-43 ('VelZ задаётся один раз и никем не меняется' — рикошет эту предпосылку отменяет). ⚠ Т18 наследует Т19 два долга, оба заведены задачами: app-2wt5 (ветка 'пол за отвергнутым барьером' без свидетеля — BarrierHeightTests не собирает кандидата-пол НИ В ОДНОМ из 14 тестов) и app-h1qn (устаревшие адреса в доках). ⚠ И честно: у ProjectileFlight сегодня ОДИН продакшен-вызывающий; второй (клиентский трассер) придёт в Т32, до тех пор 'единый дом' — обещание формы, а не факт двух вызывающих.
⛔ ПОПРАВКА К ПРЕДЫДУЩЕЙ НОТЕ, без смягчения: я назвал id задач ДО их создания и промахнулся. Верные id — app-rahx (ветка 'пол за отвергнутым барьером' без свидетеля, P3) и app-pvno (устаревшие адреса в доках, P4). Названные в предыдущей ноте 'app-2wt5' и 'app-h1qn' НЕ СУЩЕСТВУЮТ — id выдаёт bd при создании, и предсказывать его нельзя. Урок тот же класса 569: идентификатор из головы — не идентификатор из трекера.
Т19 закрыт (5e75873). Фаза Ф2 прошла ВОСЕМЬ тасков из двенадцати (Т12-Т19). Rulings 92-99, находки Н-35 и Н-36, решение владельца Р439 (отклонение 10 не исполняется) и Р438 (скоуп сессии 69 - только Т19). Дальше Т20 (пробитие мелких целей). ⚠ Т20 наследует от Т19: (1) маркер-ключ WeaponConfig теперь RicochetMinSpeed, и два поля Т20 снова его двигают - четыре вещи и ПЯТЬ аргументов EnsureAssetHasKey, а не один; (2) ProjectileFlightTests.cs существует, Т20 дописывает в него пять тестов; (3) фикстура Т20 берётся от Open(), а не Quiet() - у неё свои тела, а не препятствия; (4) Ruling 94 действует и на Т20: если пробитие меняет исход существующего теста, фикстура заявляет своё число сама; (5) урон живёт на самом снаряде (ProjectileState.Damage), а МАССА выводится из владельца (Impact.ProjectileMassFor) - для второго типа патрона масса обязана переехать на снаряд, это уже числится side-quest'ом Т37.
Т19, итог для эпика (⚠ колонка notes у app-88jb ПЕРЕПОЛНЕНА, bd note туда не проходит - см. app-x ниже). РУЛИНГИ 92-99. Ruling 92: рикошет вторым публичным членом ProjectileFlight, Step не тронут ни строкой (89 вставок, 0 удалений) - его собственный док запрещает это тремя пунктами контракта. Ruling 96: посадка контакт+Geometry.Skin, ДОКАЗАНА МУТАЦИЕЙ M17 (голый контакт садится на 2.4e-07 внутрь и гасит раунд о собственную точку посадки; число предсказано до прогона и сошлось до седьмого знака). Ruling 97: развилка владелец->числа рикошета получает один дом. Ruling 98 (находка исполнителя): отскок сажает и высоту. Ruling 99: опт-ауты закрыты доком. Ruling 95: проверка 'контакт внутри другого барьера' недостижима - задача app-3rvm. ⛔ Н-36 + Р439: десятое отклонение спеки НЕ исполняется - буква плана сносила три пина несущих инвариантов, один невосстановим. ⛔ Н-35/Ruling 94 (п.1 исправлен по спеке): фикстура несёт 1, игра 2. ⭐ R-APPLY: +15/-24 против предсказанных +15/-0 - 24 удаления суть мёртвые ключи зонной колонки, ушедшие сами по механизму Ruling 44/52; осталось 6 в HeroConfig.asset + AimProxyHeadRadiusFrac. ⛔ Два свипа за минуту до коммита нашли русскую прозу в TestConfigs.cs и три британизма - ни один из четырёх диспетчей их не назвал. Исполнитель поправил координатора 13-й раз подряд: 14 несогласий, 11 приняты, 2 отвергнуты.
Круг правок по ревью Т19 закрыт (97213dd). Ревью дало Critical ноль, три Important, пять Minor. Закрыты I-1 (пять свидетелей + семь мутаций), I-2, I-3, M-1, M-2, M-4, M-5. M-3 (modelled) НЕ правится — решение, довод app-215e. ⭐ Ruling 100: remedy в ледгере обещал три теста и три мутации — счёт мой был неверен: посадка высоты это ДВА отдельных присваивания (PrevHeight и Height), наблюдаемых порознь, плюс два теста M-5 тоже обязаны иметь жертву. Итог — пять тестов, семь мутаций M20a-M20g, 7/7 убиты, у каждой ровно одна ПРЕДСКАЗАННАЯ жертва с предсказанным ассертом. ⭐ Приём к переносу: утверждение 'высота села на контактную' вилкой не доказывается — вилка убивает мутанта-застоя, но НЕ мутанта 'сдвинуть на весь шаг'; тест восстанавливает долю шага t из ГОРИЗОНТАЛИ (посадка минус скин), которую сам не измеряет, и сверяет вертикаль против неё, с премисой 0<t<1 строго. Тестов 1653 -> 1658, зелёных 1655, красных ровно три — эталоны, значения совпали с 5e75873 до последней цифры. Мутаций эпика 58 -> 65. Продакшен-строк в коммите НОЛЬ (проверено фильтром по диффу, не заявлением). ⚠ Урок 575: состояние дерева, снятое ДО подтверждённой остановки субагента, — не состояние дерева: точка возобновления называла 4 файла, дерево несло 7, три дописаны в окне между съёмом и TaskStop. ⚠ M20a: предсказано -4.8000002, машина -4.8000000715255737 — рантайм считает цепочки float в широкой точности; вердикт не задет, но предсказание было строже машины. ⚠ Пятое законное совпадение свипов: свип зонной колонки даёт одну строку HitZoneTests.cs:139 LegsHit_AmountIsLegsMult — это ИМЯ теста, не ссылка на снятое поле, и оно старше круга. Заведено: app-c4pz (P3) — посадка рикошета по ГОРИЗОНТАЛИ ничем не запинена, ассерт неравенственный. Дальше Т20.
Т20 закрыт зелёным и 22/22 мутациями (коммит ниже). Сессия 70, скоуп — только Т20 (решение владельца Р440). Фаза Ф2 прошла ДЕВЯТЬ тасков из двенадцати (Т12-Т20). РУЛИНГИ 101-105, находка Н-38.

⭐ Н-38 (Critical, поймана аудитом до кода): два поля пробития идут в ОБА SO-класса (WeaponConfig И MobConfig), а не только в оружейный. Три источника против одного: таблица стартовых чисел спеки (:300-301) и строка Files плана (:3525, глоб Data/*Config.cs) против Global Constraints плана (:149-150). ⇒ маркер-ключ едет у ПЯТИ ассетов. Ruling 101: развилка «кто выпустил → числа пробития» получает один дом в Impact (PierceNumbers + PierceNumbersFor + Pierces), но проверка i-frames остаётся на месте вызова — она про «долетит ли удар», а не про «пробивает ли раунд». Ruling 102: предикат остаётся СТРОГИМ по букве спеки (dmg > Hp), хотя смерть в коде считается нестрого (Hp -= dmg; Hp <= 0, то есть dmg >= Hp) — условие спеки строже, и док обязан звать это строгим перебоем, а не смертью цели; граница получила свидетеля. Ruling 103: пробивший раунд садится в контакт и сажает высоту, скин НЕ ставится (у телесного контакта нормали нет — hitDir там собственная скорость раунда), плюс гейт TTL в ветке пробития (она обходит default:, где живёт единственная проверка Ttl <= 0). Развилка A закрыта: веток пробития ДВЕ (HitMob и HitPlayer), жертва читается копией до удара.

⛔ Ruling 104 (находка координатора в зелёной фазе, ОТХОД ОТ БУКВЫ ПЛАНА): три фикстуры Т20 морозили тело швом mi.Ai = MobAiState.Idle, который файл САМ объявляет пустым — док ProjectileFlightTests.cs:892 пишет «FROZEN BY ITS NUMBERS, NOT BY ITS FSM STATE (Ruling 17)». Шов пришёл из САМОГО ПЛАНА (:3565, :3612, :3641) — исполнитель скопировал дословно и был прав. Ruling 17 сверен по ледгеру (progress.md:880-901): на Т4 этот шов был СНЯТ как доказуемый no-op. Четыре теста приведены к заморозке числами (пятым был HeavyEnoughRound_PiercesAKillShot — единственный, кто не морозил вовсе). Проверено прогоном: после правки все четыре класса остались зелёными ⇒ фикстуры на движение тел не опирались, снята ложная премисса и ничего не сломано.

⭐⭐ Ruling 105 (вторая находка, и она дорогая): ветка i-frames в case HitPlayer не имела НИ ОДНОЙ жертвы — ни один из девяти тестов пробития не поднимал рывок, мутация «снять условие» пережила бы весь сьют. Дописан десятый тест RoundEatenByIframes_DoesNotPierceTheCollector, и у него ДВА утверждения намеренно: у мутанта две формы, и каждое ловит только одну. М-13 показала, какая именно случается — счётчик контактов дал 5 (раунд пробивает ЖИВОГО сборщика, садится в контакт, встречает его снова, делит урон 1000→500→250→125→62.5 до отказа перебоя против MaxHp 100), а MobCount при этом ОСТАЛСЯ 1. ⇒ очевидная одноутверждённая версия теста эту мутацию пропустила бы. Тем же числом подтверждён довод, которым закрыта развилка A: это была гипотеза по коду, теперь это измеренное 5.

ЧИСЛА. Тестов 1658 → 1672 (+14: девять тестов пробития исполнителя, три негативных правила 10, свидетель i-frames, свидетель развилки владельца). Полный R-TEST: EXIT=2, зелёных 1669, красных РОВНО ТРИ — эталоны, и ⭐ НИ ОДИН НЕ СДВИНУЛСЯ (предикат П-2, обратный к Т19): ExtractionGoldenHash 17873972842010472628, GoldenHash 5616473150957384349, MultiPlayerGoldenHash 11176157811214132580. Неподвижность была предсказана арифметикой: пробитие открывается телу легче ProjectileMass/PierceMassRatio = 43.33 кг (раунд сборщика) и 50.00 кг (мобий), а самое лёгкое тело фикстуры — ганнер 70 кг. Гейт Р433: 337.34 c, Determinism+Allocation = 97.8 % против 97.7 % на Т18/Т19 — доля не двинулась; абсолют в эпике гуляет 271-330 c на неизменном коде (ледгер называет причиной загрузку машины). AllocationTests.Tick_DoesNotAllocateGC зелен — единственный свидетель решения объявить PierceNumbers readonly struct, и фильтры его не гоняли ни разу.

МУТАЦИИ 22/22, и КАЖДАЯ убита НАЗВАННЫМ ассертом; у тринадцати из двадцати двух сьют указал в РОВНО ОДНУ точку. Мутаций эпика 65 → 87. ⭐ Сухая проверка якорей ДО цикла нашла дефект в самом списке: М-10 не имела уникального якоря, потому что посадка высоты (PrevHeight/Height) написана в ProjectileFlight ДВАЖДЫ — у рикошета и у пробития; без сухого прогона это стоило бы второго цикла. М-5 предсказала СОПУТСТВУЮЩЕЕ падение (контрольная половина теста сборщика) — упали ровно два теста. М-7 предсказала жертву ассертом 1, а не 3, и это разбиралось до прогона: без урезания урона гибнет и дальнее тело, MobCount падает раньше, чем дело дойдёт до числа здоровья. Довесок М-21/М-22 добавлен по букве плана (Step 5a просит ОСЛАБИТЬ границу ReqPositive→ReqNonNegative, а не снять правило: снятие доказывает, что правило есть, ослабление — что граница строгая).

R-APPLY сошёлся с предсказанием ДО СТРОКИ, в отличие от Т19: пять файлов, +10/−0, значения 0.06 и 0.5 во всех пяти, сцена 269 / 0241c714945a15bf1c68060f075209a4 не двинулась. Незаказанных удалений НЕТ — долг мёртвых ключей зонной колонки закрыл ещё Т19; остаток 6 в HeroConfig.asset держится, его маркер не двигался. ⚠ Явного SetIfDifferent для двух новых чисел НЕТ, и это форма Т19, а не недосмотр: стартовые числа спеки совпадают с C#-дефолтами, явный сеттер был бы вторым домом того же числа. Цена названа честно: у поля без сеттера ассет перестаёт следовать за C#-дефолтом, как только ключ в нём появился.

⭐ ПРИЁМ К ПЕРЕНОСУ, прямо в app-c4pz: PiercedRound_LandsAtTheContactAndKeepsItsDescent пинит посадку по ГОРИЗОНТАЛИ неравенствами 0 < t < 1 строго, где t восстановлена из позиции, — и это убило ОБЕ мутации порознь (М-8 «остался на дошаговой», ассерт 9; М-14 «проехал весь шаг», ассерт 10). app-c4pz говорит ровно о том, что у рикошета горизонталь запинена неравенством >= r+pr и мутанта «вовсе не двигать Pos» переживает. Приём готов к применению один в один.

⚠ Т21 наследует: (1) маркер-ключ обоих SO-классов теперь PierceDamageLoss — следующее новое поле снова двигает его у ПЯТИ ассетов; (2) ProjectileFlight несёт ТРИ публичных члена (Step, TryRicochet, TryPierce), и посадка высоты написана в нём дважды — при третьей механике это кандидат на общий хелпер; (3) масса снаряда по-прежнему выводится из владельца (Impact.ProjectileMassFor), для второго типа патрона обязана переехать на снаряд — side-quest Т37; (4) конвенция премисс в ProjectileFlightTests — английское «fixture premise:», русский там язык УТВЕРЖДЕНИЙ (Т20 писал обе формы, восемь нормализованы).

Круг правок по ревью Т20 закрыт (06d7d07). Ревью (Explore, fable) дало Critical 1, Important 3, Minor 3 — все семь перепроверены координатором лично по файлам и состоятельны ВСЕ СЕМЬ, ни одна не отвергнута.

⛔⛔ C-1 (Critical) — САМАЯ ДОРОГАЯ НАХОДКА СЕССИИ, И ОНА ПРО ПРОВОД, А НЕ ПРО СИМУЛЯЦИЮ. Пробивший раунд эмитил ProjectileHit / ProjectileHitPlayer, а на проводе эти два вида означают КОНЕЦ раунда. Цепочка проверена целиком, каждое звено открытием файла: эмит безусловен (ProjectileSystem.cs:344,:430) → контракт вида «EMITTED ON EVERY REMOVAL… It reports that the ROUND ENDED» (SimEvents.cs:58-59) → ассемблер маппит оба в ProjectileEnded по id раунда (SnapshotAssembler.cs:639-663) → маршрутизация делает Enqueue + QueueUnsubscribe(c, we.RoundId) (:1138-41) → клиент делает _ghosts.TryTranslateEnd и _tracers.Retire (NetworkSimBackend.cs:2481,:2537). Следствие: пробитый контакт сообщал всем зрителям «раунд кончился», ОТПИСЫВАЛ их от его id, клиент гасил трассер — а сервер вёл раунд дальше; его настоящий конец адресовался множеству, где уже никого нет, а MobDied, чья маршрутизация делает union подписчиков убившего раунда, объединял ноль.

⭐⭐ Дефектом, а не развилкой, это делает Т19: его ветка рикошета зовёт TryRicochet ПЕРЕД эмитом и на успехе делает break ДО него, а док пишет дословно «NO EVENT ON SUCCESS. ProjectileRicocheted is Т30's; until then a reflection is silent on the wire». Т20 оставил эмит перед развилкой и сделал условным только снятие — отход от формы соседнего таска в том же switch.

RULING 106: решение о пробитии переехало ВЫШЕ эмита в обеих телесных ветках, эмит взят под тот же предикат, что и снятие. Инвариант восстановлен — событие эмитится ровно там, где раунд снимается, и нигде больше. ⭐ Ничего не теряется, и это проверено по коду, а не предположено: события MobDamaged в проекте НЕТ ВОВСЕ (ProjectileHit и есть событие урона по мобу), но пробитие требует строгого перебоя, поэтому за каждым пробитым мобом идёт MobDied (несущий урон убившего удара), а за пробитым сборщиком — PlayerDamaged (его эмитит сам DamagePlayer) и PlayerDied. Событие СЕРЕДИНЫ жизни у пробития по-прежнему отсутствует, как и у рикошета до Т30 — заведено app-tbvg (discovered-from app-50db), id взят из трекера, а не придуман.

⚠ ПОЧЕМУ ЭТОГО НЕ НАШЁЛ Я, без смягчения. Я ЗНАЛ ключевой факт и пользовался им: свидетель i-frames построен именно на том, что ProjectileHitPlayer эмитится ДО DamagePlayer. Но я проверил только, ГДЕ стоит эмит, и ни разу не спросил, ЧТО С НИМ ДЕЛАЕТ ПРОВОД — аудит адресов охватывал Simulation, а Networking в него не входил вовсе. Урок к переносу: таск, вводящий «раунд переживает контакт», обязан пройти по ВСЕМ потребителям следа этого контакта, а не только по месту, где ветка написана.

Остальные шесть: I-1 — три дока, ставших ложью от прошлого коммита (⭐ половина испарилась от починки корня: доки SimEvents снова истинны, потому что инвариант «событие = конец» восстановлен); I-2 — три соседних теста несли отменённую премиссу, ProjectileTests.PlayerShot_NoPiercing переименован в ShippedNumbers_PlayerShotDoesNotPierce (это больше не правило, а факт о стартовых числах: 2.6/90 = 0.029 против порога 0.06), плюс ProjectileHeightTests.cs:378 и PvpDamageTests.cs:799; I-3 — словарь Ruling 102 нарушен в четырёх местах («killing blow» / «СМЕРТИ цели» / «damage that KILLS» → строгий перебой, с явным «ровно смертельный удар раунд гасит»); M-1 — три соврамших счётчика в доках (21→22, «SIX TESTS»→ELEVEN, «all five»→all nine that fire at all); M-2 — сторож стартовых чисел мерил ОДИН ствол, теперь делит массы и на Gunner.ProjectileMass против Gunner.PierceMassRatio (без этого балансовая правка мобьей массы выше 4.2 включила бы механику для всего бестиария БЕЗ ЕДИНОГО красного теста); M-3 — мобья половина верхней границы PierceDamageLoss была без свидетеля, дописан Validate_MobPierceDamageLossAtOne_Throws с нарушением на ВТОРОМ архетипе.

СВИДЕТЕЛИ САМОЙ ПРАВКИ — три утверждения, и они ДВУСТОРОННИЕ: PiercedRound_LosesTheConfiguredShareOfItsDamage ассерт 2 (CountOf(ProjectileHit) == 1), HeavyRound_PiercesACollector контрольная половина ассерт 10 (== 1) и пробивающая ассерты 13/14 (== 0 и == 0). Гейт, залипший открытым, и гейт, залипший закрытым, — разные дефекты, одно число ловит только один.

МУТАЦИИ КРУГА 5/5, каждая названным ассертом. ⭐⭐ Самая ценная проверка — ОТРИЦАТЕЛЬНАЯ, и она сошлась: список предупреждал ДО прогона, что М-25 (перевёрнутый мобий гейт) ПЕРЕЖИВЁТ PiercedRound_Loses… — там он даёт то же число 1 (ближнее тело пробито и эмитит, дальнее не пробито и молчит). Факт: этого теста в красных М-25 нет, мутанта поймал RoundThatDoesNotKill_DoesNotPierce, ровно назначенный. Двусторонность пришлось распределить по двум тестам не из осторожности, а по арифметике. Мутаций Т20 — 27, эпика 65 → 92.

ЧИСЛА: тестов 1672 → 1673. Полный прогон круга: EXIT=2, зелёных 1670, красных три — те же эталоны на тех же числах (сдвига не ждали и по дополнительной причине: гейт эмита меняет только испускание событий, а события в StateHash не входят вовсе, SimEvents.cs:7). Финальный фильтр на точных байтах коммита: ProjectileFlightTests 22/22, ImpactConfigTests 16/16, error CS 0. ⛔ Свип за минуту до коммита снова дал НЕ ноль — русская проза «круг правок» в комментарии кода (правило 454); это второй раз подряд, когда именно последний проход находит то, чего не нашёл ни один диспетч.

⚠ Т21 наследует дополнительно: (1) эмит в обеих телесных ветках теперь под гейтом пробития — третья механика, продлевающая жизнь раунда, обязана попасть под тот же гейт, иначе повторит C-1; (2) app-tbvg ждёт Т30; (3) нумерация ассертов в HeavyRound_PiercesACollector сдвинулась на единицу ниже десятого — прежние эвиденсы верны для того кода, на котором сняты, и не переписываются.

Т21 (Geometry.ResolveBodyPair) — зелёная фаза и мутации закрыты, сессия 71.

СДЕЛАНО. Чистая функция симметричного разведения двух тел: два out-смещения, лёгкое
тело уступает больше по отношению масс, тай-брейк полного перекрытия — от id, не от
оси X. Дифф — чистая вставка: Geometry.cs +114/-0, GeometryTests.cs +120/-0,
удалений ноль в обоих. Тестов в GeometryTests 51 -> 56, по проекту 1673 -> 1678.

TDD. RED: EXIT=2, красных два — предсказанные, на ассерте 1 каждый, error CS 0, Csc 8.
GREEN: EXIT=0, 56/56, error CS 0, ГЕЙТ-ЛОГ 0, ГЕЙТ-ОТКАТ 0.
ПОЛНЫЙ R-TEST: EXIT=2, total 1678, зелёных 1675, красных РОВНО ТРИ — эталоны, и их
числа НЕ СДВИНУЛИСЬ (17873972842010472628 / 5616473150957384349 / 11176157811214132580).
Четвёртого красного нет. Довод был фальсифицируем заранее: у функции ноль вызывающих.

МУТАЦИИ: 9 (M-1..M-8 + M-S2). Убиты восемь, каждая НАЗВАННОЙ жертвой и НАЗВАННЫМ
ассертом; M-S2 (значение эпсилона) выжила, как и предсказано — свидетеля не получает
по ruling 112, долг несёт app-knyr. Счёт мутаций эпика 92 -> 100.

ГЛАВНАЯ НАХОДКА ТАСКА (ruling 111). Тест 54 «тай-брейк по id, а не по оси X» на деле
пинил только ЗНАК, а не НАПРАВЛЕНИЕ: мутант «жёстко (1,0), но со знаком от id»
проходил все три его ассерта. Проверено счётом лично, до прогона, и подтверждено
фактом — M-8 убита ТОЛЬКО новым пятым тестом, тест 54 её пережил. Дописан пятый
свидетель: две РАЗНЫЕ пары id обязаны разойтись в РАЗНЫЕ стороны.

РУЛИНГИ 107-112: Skin не входит в величину разведения (иначе копится по парам);
тай-брейк = симметричный по паре угол × антисимметричный знак, math.radians (Deg2Rad
из плана — UnityEngine.Mathf, запрещён CR 1); мутации снимаются в Т21, не в Т22;
граница overlap <= 0 получает свой сторож; пятый свидетель; эпсилон — задачей.

ЗАВЕДЕНО СВЕРХ: app-rw2l (idA == idB достижим в Т22), app-atm9 (mA+mB == 0 -> NaN),
app-knyr (значение эпсилона без свидетеля), app-9ciy (P1 — арифметика float в
редакторе повышается до double, эталоны детерминизма сняты в редакторе).

ДВА ИНСТРУМЕНТАЛЬНЫХ ФАКТА, ВЛИЯЮЩИХ НА ВСЕ БУДУЩИЕ ТАСКИ.
1. timeout на этой машине — uutils coreutils 0.2.2, и её страховка НЕ РАБОТАЕТ:
   -k даёт код 125 и оставляет ребёнка жить, -s KILL не срабатывает вовсе. Ровно это
   дало семичасовое зависание Unity под мутацией M-3. Прогоны переведены на
   собственный сторож: фон + точный PID + свой потолок + kill -9 по номеру.
2. Редактор считает float с повышением до double (Mono -O=-float32) — предсказания
   чисел в мутациях обязаны считаться в этой семантике, иначе мимо в 8-м знаке.

ЭВИДЕНС: .superpowers/sdd/2026-08-24-app-88jb-weapon-netcode/ — task-21-red-evidence.md,
task-21-mutations-evidence.md (девять мутаций + разбор расхождения чисел до механизма),
task-88jb-21-mutations-predicted.md.

Т21 — ревью таска и круг правок, коммит 746d50d.

Ревьюер (Explore/fable) вернул четыре находки, все «мелочь», блокеров нет.
Проверены мной лично по файлу, не приняты на веру.

ИСПРАВЛЕНО ТРИ (правки только в комментариях, строк кода в диффе ноль):
1. Комментарий сторожа ExactTouch считал тесты выше «три» — их четыре: рулинг 111
   вставил пятый тест ИМЕННО перед сторожем. Таск нашёл док-ложь в чужом тесте и тем
   же движением завёл свою; поймало только ревью. Следствие на будущее: вставка теста
   в середину файла требует проверки комментариев, которые считают соседей.
2. Дока ResolveBodyPair записала предпосылку про idA != idB, но молчала про массы,
   хотя делит на mA + mB. Предпосылка записана в стиле файла; долг — app-atm9.
3. «The three other pushes» перечисляло ClampInsideRing и пропускало PushOutOfArc.
   Списки PushOutOf* (три: :583, :607, :663) и мест применения Skin (три: :593, :627,
   :730) НЕ СОВПАДАЮТ. Переформулировано на то свойство, что имелось в виду.

ОТКЛОНЕНА ОДНА, с основанием: ассерты 2-3 сторожа действительно не имеют собственной
мутации (M-7 убивает их вместе с NoOverlap), но правило эпика — «мутация без жертвы =
находка», а не «ассерт без своей мутации = лишний»; они закрепляют контракт «отказ ⇒
нули» на ДРУГОМ входе (касание ровно в точку), и их слабость исполнитель назвал
заранее в предсказании M-4.

ПРОГОН ПОСЛЕ ПРАВОК: 56/56, EXIT=0, error CS 0, ГЕЙТ-ЛОГ 0, ГЕЙТ-ОТКАТ 0, Csc=2.
Свипы прогнаны повторно по staged-байтам — все нули.

⚠ Номера строк в Geometry.cs ниже :735 сдвинулись на 11; прежние эвиденсы верны для
того кода, на котором сняты, и не переписываются.

Сессия 72, Т22 — решения владельца и аудит.

Р441: подкат НЕ проходит ни под кем; правило «проход разрешается частями» не пишется. Ветка недостижима по построению (ValidateParts требует Parts[0].Bottom==0 и контигуальность ⇒ профиль [0,0.55] пересекает части ВСЕГДА), свидетеля у неё нет.

Р442: толчок телом — закон сохранения импульса, не плоское DashPushSpeed.
  цель += share*сближение, где share = m_толкателя/(m_толкателя+m_цели)
  толкатель -= PushRecoilFraction*(1-share)*сближение
Сближение — проекция на нормаль контакта. Дом — Impact, рядом с VelocityDelta.
PushRecoilFraction: сборщик 0.25, мобы 1.0. Закон на все ТРИ пары.
Потолка нет (Ruling 114): доля < 1, движение мобов заметающее, ИИ гасит 1.0 м/с за тик. ImpactSpeedCap остаётся при пулях, где формула ничем не ограничена.
Числа: дэш толкает чейзера на 17.1 м/с, подкат 7.7, бег 4.3; Директора 0.87/0.39/0.22. Подкат сквозь троих: 13.5 -> 12.05 -> 10.76 -> 9.61 при беге 7.5.

Ruling 113: PushableBody НЕ несёт скорости — на проводе её нет (MobRecord 9 байт). Правило «сборщик не получает толчок от чужого движения» становится невыполнимым за отсутствием данных, а не документированным.
Ruling 114: потолок снят, обоснование выше.
Ruling 115: ResolveBodyPair получает out float2 normal — вырожденное направление Ruling 108 не переписывается второй раз.
Ruling 116: в паре со сборщиком Id не читается (у сборщика его нет — app-rw2l; у клиента лоссовый u16-код).

План Т22 приведён в соответствие: DashPushSpeed вычеркнут, правило «частями» отменено, Step 3a заменён семью свидетелями закона, добавлен Step 4 (Impact.ResolveBodyPush), мутации M27-M32, total 1651 -> 1694.

Аудит адресов (урок 543): 14 сошлось, 4 расхождения. Несущие: A-3 — маркер-ключ HeroConfig это МАССИВ Parts[], у MobConfig скаляр; A-4 — Т22 делает ложными ДВА текста, план назвал один (второй — порядок тика SimulationWorld.cs:383-388).

Кода не написано, дерево чистое.
Т22 ЗАКРЫТ КОДОМ — коммит 1fca552 в ветке feature/app-88jb-weapon-netcode.

ЭВИДЕНС (полный прогон, собственный сторож):
- тестов 1696, зелёных 1693, красных РОВНО ТРИ — эталоны детерминизма, error CS 0
- новые числа эталонов: Extraction 9223590669344026349, Golden 9595197576853725280, MultiPlayer 8810226901549957896 (сдвиг обязателен: Т22 меняет хеш конфига и движение тел; старые пины не трогались, санкция на перепин цела)
- мутаций 15, убиты ВСЕ 15; счёт эпика 100 -> 115
- гейты: MUTATED/ABLATED 0, MEASURING вне AllocationTests 0, UnityEngine в Simulation 0, Deg2Rad в Simulation 0, русская проза в комментариях 0, секреты 0, .meta у обоих новых файлов есть, оба UTF-8 без NUL
- R-APPLY/R-IDEM: 8 вставок в 6 ассетов как предсказано, второй прогон не добавил ни строки

ЧТО СДЕЛАНО СВЕРХ ПЛАНА (решения владельца этой сессии):
Р441 — подкат не проходит ни под кем, правило «частями» отменено.
Р442 — закон сохранения импульса вместо DashPushSpeed, на все три пары, без потолка.
Р443 — импульсные двигатели костюма: SlideSpeedPenalty + Hero.SlideThrustRecovery 18.

РУЛИНГИ 113-118: PushableBody без скорости и без id (структурный инвариант предсказания); потолка у толчка нет; нормаль out-параметром; тай-брейк не читает id в паре со сборщиком; удар ограничен фактическим перекрытием; вход парного скана заморожен на тик.

НАХОДКИ Н-41..Н-44: ложный свидетель в плане; отдача была инертна в подкате и дэше; замедление втрое (1350 мобов x 4 прохода); мили-замах короче собственного тела — заведено правило валидации, нашло два таких тела сразу.

ТРИ ЛОЖНЫХ СВИДЕТЕЛЯ, вскрытых мутациями: мерили конечное расстояние, абсолютную скорость и работу потолка вместо проекции. Урок: тест обязан мерить величину, которую не может произвести ничто, кроме проверяемого правила.

НЕ СДЕЛАНО НАМЕРЕННО: сетевая проводка набора видимых тел — app-njmi (P1), клиент в сети пока получает пустой набор.
КРУГ РЕВЬЮ Т22 ПРОЙДЕН (сессия 73, решение владельца 1a — ревью первым, до Т23). Ревьюер Explore/fable, наряд task-22-review-brief.md, эвиденс task-22-review-evidence.md. Вердикт: (A) соответствие — сделано практически целиком, лишнего нет; (B) Critical 1, Important 5, Minor 10. Все шестнадцать перепроверены координатором ЛИЧНО открытием файлов: состоятельны ВСЕ, ложных ноль; у двух счёт поправлен (M-4 продакшен-вызывающих два, не три; M-9 потолок написан дважды, не трижды — наблюдение, не дефект); M-10 не подтверждена и не отвергнута чтением, требует замера.

⛔⛔ C-1 (Critical) — ПАРА СБОРЩИК-СБОРЩИК СКАНИРУЕТСЯ ДВАЖДЫ, и при перепроверке находка ОКАЗАЛАСЬ СИЛЬНЕЕ, чем подана. CollectorPass гоняет Accumulate по КАЖДОМУ живому сборщику со снимком ВСЕХ тел и разливает реципроки по всем слотам, включая слоты других сборщиков ⇒ пара обрабатывается в проходе p и ещё раз в проходе q. ТРИ следствия: (1) позиция УДВАИВАЕТСЯ — dA_q = dB_p по симметрии ResolveBodyPair, каждый получает полное перекрытие вместо своей доли; (2) при dist<=1e-6 ключи двух сборщиков РАВНЫ (TieKey из битов массы, масса одна), знак тай-брейка не флипает, обе половины взаимно уничтожаются — два сборщика в одной точке НЕ РАЗЪЕЗЖАЮТСЯ НИКОГДА; (3) сборщик q получает толчок, выведенный из СКОРОСТИ p — ровно то, что Ruling 113 объявил НЕВЫПОЛНИМЫМ 'за отсутствием данных': данные приносит чужой проход. Клиент этого не воспроизводит по построению (PlayerPrediction.CollectorPass передаёт Empty оба реципрока) ⇒ после закрытия app-njmi каждый контакт двух сборщиков даёт рывок реконсиляции ПО ПОСТРОЕНИЮ.
⭐ ЧЕТВЁРТОЕ следствие, поднявшее уровень до Critical: док самого кода утверждает обратное. BodySeparation.TieKey:188-191 дословно — 'the pair is scanned ONCE, in slot order, by both sides' — и это ЕДИНСТВЕННЫЙ записанный довод, почему равные ключи безвредны. Он против собственного кода ложен.
⭐ УЛИКА: фикстура PredictionAndServerAgree_WhenNoBodyIsVisible_Guard НАМЕРЕННО уносит второго сборщика на 60 м с комментарием 'otherwise they would separate FROM EACH OTHER' — взаимодействие было известно и обойдено, а не проверено. Тестов на третью пару в сьюте НОЛЬ, при том что спека §3.5 называет её обязательной, а MVP это три сборщика FFA. Ни одна открытая задача этого не покрывает.

IMPORTANT: I-1 труп с нулевым радиусом ВСЁ РАВНО толкает живых (overlap=(rA+rB)-dist положителен при dist<0.45; комментарий 'radius 0 can never overlap' арифметически ложен); I-2 комментарий порядка тика SimulationWorld.cs:441-445 НЕ пересогласован — проверено ДИФФОМ, строка бит в бит родительская, хотя аудит сессии 72 (A-4) сам себе назначил 'пересогласовать ОБА'; I-3 сервер разводит от ВСЕХ тел, а спека §3.5 требует 'то же самое правило' видимости — отступление живёт только в комментарии теста, ни в ледгере, ни в §6c его нет; I-4 RunIntoBody ставит Ai=Idle и НЕ обнуляет MaxSpeed/Accel, а Idle моба не морозит (Ruling 17/104) ⇒ канал 'бег' меряет собственную локомоцию чейзера, и мутация 'ветка по режиму движения' (запрещённая Р442 дословно) пережила бы сьют — ТРЕТИЙ заход того же семейства за таск: сессия починила это в PushedMob (M31 заход 2) и оставила в общем хелпере, кормящем три теста; I-5 порог dash>slide*1.3 против плановых 1.5 подвинут ПОД ФАКТ молча, в ледгере вхождений '1.3' ноль.

MINOR подтверждены восемь: дублированный док-блок 'THE HALF THAT MATTERS' (:327 и :350) плюс ложные утверждения про Директора на БЕЗЗОННОЙ OpenField; '0.97 m' — это доля 4000/4120, а не метры (реально 2.48 м); мёртвая ссылка на снятый _pushSlot; дока ResolveBodyPair описывает закрытие app-rw2l неверно (обещает 'id space disjoint', а Ruling 116 закрыл неперечитыванием Id); имя awayFromPusher перевёрнуто (передаётся нормаль К толкателю); §6c спеки не содержит Р443; ДВЕ строки русской прозы при заявленном гейте '0', первая пришла дословно из плана; ледгер пишет интервал провала (11,12.5), код — Less(13.0).

⚠ КРУГ ПРАВОК НЕ НАЧАТ: по команде владельца сессия останавливается на границе ревью и готовится к компакту. Кода не тронуто ни строки, оба дерева чистые (0 строк), HEAD=1fca552, снимок снят ПОСЛЕ подтверждённой остановки субагента (урок 575).

⚠ РАСХОЖДЕНИЕ HANDOFF'А С ТРЕКЕРОМ, найдено на старте: §1 handoff'а пишет 'app-rw2l ЗАКРЫТА таском Т22', в bd она ОТКРЫТА. Ruling 116 снял проблему по существу, но задачу не закрыл никто.
КРУГ ПРАВОК ПО РЕВЬЮ Т22 ЗАКРЫТ — коммит 398dda8 в ветке feature/app-88jb-weapon-netcode (запушен, сверен живым ls-remote).

RULING 121 — ФОРМА ЛЕЧЕНИЯ C-1 ВЫВЕДЕНА, А НЕ ВЫБРАНА. Точка возобновления оставляла три кандидата и вопрос владельцу; вопроса не осталось, три ограничения сходятся на одной форме. (1) Паритет предсказания (спека 3.5 + CR 3): клиент считает ТОЛЬКО свою половину, значит слот сборщика на сервере обязан получить ровно dA своего прохода и ничего сверх. (2) Ruling 113: сборщик не получает толчок от чужого движения. (3) Если просто перестать разливать реципроки, у обеих сторон вырожденной пары окажется ОДИНАКОВАЯ нормаль (ключ из битов массы, у двух сборщиков равен, знак не флипает) — и пара поедет через арену ВМЕСТЕ по MaxDepenetrationPerTick за тик, что хуже сегодняшнего залипания. ⇒ Две вынужденные правки: реципроки уходят ТОЛЬКО мобам (k < mobCount), вырожденная пара с равными ключами ОТКЛОНЯЕТСЯ в ResolveBodyPair. Четвёртый кандидат («дать сборщикам различимый ключ») отвергнут структурно: различить их НЕЧЕМ — PushableBody несёт Pos/Radius/Mass, при полном перекрытии позиции равны, радиус и масса у героев из одного HeroSimConfig; любой различающий ключ — это направление, которого у клиента нет. Непроверяемая предпосылка функции стала проверяемой, док перестал быть ложью.

СДЕЛАНО: C-1 (спилл + сторож + два лживых дока переписаны с записью, чем врали); I-1 (Accumulate пропускает Radius <= 0, обоснование в SnapshotBodies переписано); I-2 (комментарий порядка тика пересогласован); I-3 (ЗАПИСЬ в 6c спеки: почему сервер не фильтрует по видимости — в Simulation нет VisibilitySet, CR 1 запрещает читать сетевой слой; и что осталось владельцу — выпадение тела по бюджету кадра); I-4 (RunIntoBody и SidewaysPush морозят тело КОНФИГОМ, Ai=Idle моба не морозит); I-5 (оба порога вернулись к плановым 1.5 разбором механизма); восемь Minor. M-9 — наблюдение, правки не требует.

⭐⭐ ЗАМЕР ВСКРЫЛ БОЛЬШЕ, ЧЕМ НАЗВАЛО РЕВЬЮ. Бок: 1.99 -> РОВНО НОЛЬ (M-10 подтверждена целиком: 1.99 было собственным разгоном чейзера, Accel 30 = 1.0 м/с за тик при трёх тиках фикстуры, а док объяснял это «поворотом нормали»; нормаль правда поворачивается — и даёт составляющую НА РАСХОЖДЕНИЕ, approach <= 0, толчка нет вовсе). ⚠ Грязен был НЕ ТОЛЬКО бег: подкат 7.71 и дэш 12.57 держались тем, что чейзер САМ ШЁЛ В УДАР и подкармливал перекрытие, снимая потолок Ruling 117 — против пассивного тела закон отдаёт 5.149 и 10.439. Механизм проверен самим же замером: у Директора доля сборщика в позиционном разведении 0.971, добавочного разведения почти нет, потолок не бьёт — и число сошлось РОВНО: 0.3932 = 0.029126 * 13.5. Дописан пол run > 1f: без него мутация «толкать только в подкате и дэше» (запрещена Р442 дословно) переживала сьют, потому что slide > run*1.5 при run=0 выполняется.

СВИДЕТЕЛЕЙ У ТРЕТЬЕЙ ПАРЫ БЫЛО НОЛЬ — дописаны пять: доля перекрытия, Ruling 113 (стоящий не получает скорость от чужого движения), паритет с клиентом на втором сборщике, вырожденная пара, труп нулевого радиуса.

ЭВИДЕНС: полный прогон 376 c, EXIT=2, total 1701 (было 1696, +5 новых), зелёных 1698, красных РОВНО ТРИ — эталоны, и все три БИТ В БИТ ПРЕЖНИЕ (9223590669344026349 / 9595197576853725280 / 8810226901549957896). error CS 0. Мутации 4/4 убиты: M22b спилл на bodyCount (три жертвы), M22c снятый сторож равных ключей, M22d снятый пропуск нулевого радиуса, M22e ветка толчка по режиму движения — жертвы поимённо как предсказано; откат сверен md5 ПОСЛЕ ALLDONE. Свипы ДВАЖДЫ: кириллица 0, британизмы 0 (второй проход поймал behaviour в моём же тексте), маркеры 0, UnityEngine 0, Deg2Rad 0, секреты 0, новых файлов 0, NUL 0. Счёт мутаций эпика 115 -> 119; у Т22 было семь, стало одиннадцать — строка гейта Ф2 в плане обновлена (29 -> 33).

⭐ УРОК 599: «эталон не двинулся» — тоже измерение. ДВА из трёх эталонов гоняют ТРИ сборщика (1000 и 18000 тиков), путь пары там достижим — и все три константы не шелохнулись. ⇒ Пара сборщик↔сборщик НЕ ИСПОЛНЯЕТСЯ НИ В ОДНОМ эталонном сценарии: три случайно бродящих сборщика ни разу не сошлись на 0.9 м за 600 секунд. Усиливает урок 597: дефект был невидим не только мутациям и свипам, но и детерминистическим эталонам. И правит запись handoff'а/точки возобновления «в golden сборщик один» — это верно лишь для GoldenHash_ScriptedScenario.
⭐ УРОК 600: предсказывать надо величину В ОКНЕ АССЕРТА, а не в тике. «Стоящий получит 6.75 м/с» промахнулось (выпало 10.67): ассерт берёт max за 20 тиков, а 6.75 — величина ОДНОГО удара; в держащемся контакте скорость копится быстрее, чем её съедает трение.

ОТКРЫТО ВЛАДЕЛЬЦУ: (1) app-rw2l — Ruling 121 закрыл её предмет по существу (равные ключи теперь отклоняются явно), можно закрывать; (2) I-3 — развилка по скоупу «заводить ли видимость внутри симуляции», факт записан в 6c, решение за владельцем; (3) Т23 к работе готов — аудит и рулинги 119/120 сняты, ожидание total пересчитано с 1698 на 1704.
Т23 ЗАКРЫТ КОДОМ — коммит d943dd1 в ветке feature/app-88jb-weapon-netcode (запушен, сверен живым ls-remote). Потолок скорости снаряда 300 м/с, правило 13 спеки 3.10.

RULING 122 (новый) — ДОМ ТЕСТОВ В ПЛАНЕ НАЗВАН НЕВЕРНО. Files Т23 велят ZoneConfigTests.cs; факт дерева: это сьют валидаций зон/дверей/порталов этапа 3 таска 8, упоминаний app-88jb в нём НОЛЬ. А ImpactConfigTests своим же класс-доком объявлен домом правил 3.10 эпика (1/6/7/8/9/10/11) и несёт РОВНО тот двухполовинный образец, которого требует рулинг 120: Validate_PierceDamageLossAtOne_Throws плюс Validate_MobPierceDamageLossAtOne_Throws. ⇒ правило 13 уехало туда, класс-док дополнен «13». ⚠ ЧЕТВЁРТЫЙ ПОДРЯД случай «план против факта кода»: Н-38 (Т20), Н-39 (Т21), рулинг 119 (пол MobConfig), рулинг 122 (дом тестов) — аудит адресов перед имплементацией окупился четвёртый раз.

СДЕЛАНО: WeaponConfig [Range(1f,300f)]; MobConfig [Range(0f,300f)] — рулинг 119, пол ноль несущий (чейзер мили, отгруженный ассет несёт 0, валидация ReqNonNegative против ReqPositive у оружия); правило 13 ОБЕИМИ половинами (рулинг 120) через новый помощник ReqAtMost. ReqAtMost — третий помощник, а не вызов ReqInRange, и это не вкус: у двух половин РАЗНЫЕ полы (> 0 у оружия, >= 0 у мобов), ReqInRange накрыл бы оба одной включающей границей и молча сделал бы отгруженный ноль чейзера незаконным. Константа ProjectileSpeedCeiling — на уровне класса: ValidateMob отдельный метод, локальная до него не достаёт (поймано компиляцией).

ЭВИДЕНС: RED total 19 failed 2, обе «But was: null» — предсказание сошлось. GREEN 19/19, error CS 0. Мутации ДВЕ, а не одна как писал план (рулинг 120 требует свидетеля обеим половинам): M42 ( ->  в ReqAtMost) убила Validate_ProjectileSpeedExactlyAtTheCeiling_IsLegal, M43 (снята мобья половина) убила Validate_MobProjectileSpeedAboveTheCeiling_Throws — по одной жертве каждая, поимённо как предсказано; откат сверен md5 ПОСЛЕ ALLDONE. Полный прогон 366 c, total 1704 (1701 после круга правок + 3), зелёных 1701, красных РОВНО ТРИ — эталоны, и ВСЕ ТРИ БИТ В БИТ ПРЕЖНИЕ (9223590669344026349 / 9595197576853725280 / 8810226901549957896). Свипы дважды — кириллица 0, британизмы 0, маркеры 0, UnityEngine 0, секреты 0, новых файлов 0.

⚠ R-APPLY/R-IDEM и ГЕЙТ-КОДОГЕН не гонялись, и это ПРОВЕРЕНО, а не предположено: Т23 не заводит ни одного поля конфига (дифф Data/ — по две строки атрибута на класс), значит маркер-ключи не двигаются; Networking/** не тронут. Строка гейта Ф2 в плане обновлена: мутаций фазы 29 -> 34 (круг правок Т22 дал четыре, Т23 дал две вместо одной).

⚠ НАЗВАННАЯ ОГОВОРКА (находка C-M9 плана, не закрыта этим таском): при 300 м/с снаряд проходит 10 м за тик, весь бой на 20 м укладывается в два тика, и правило «пробитие не более одного тела за тик» фактически перестаёт связывать. Это материал для амендмента ADR-001 §9 на Т33, а не нарушение §11 — снаряд остаётся физическим телом при любой скорости. Записано в доке правила.

ЧТО ДАЛЬШЕ И ЧЕГО ЭТА СЕССИЯ НЕ ДЕЛАЛА: гейт Ф2 (шесть целей сборки, ГЕЙТ-КОДОГЕН по четырём ScriptAssemblies и Ring.Networking.dll из артефакта релизного сервера, R-APPLY/R-IDEM, два фазовых ревьюера) и веха В2 «Прицел» — плейтест владельца, СТОП. Ждут отдельной команды владельца.
